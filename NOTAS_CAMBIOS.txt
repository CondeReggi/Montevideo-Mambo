==========================================================================
 BITÁCORA DE CAMBIOS — APP Montevideo MAMBO (Academia de Baile)
 Se registran aquí los cambios y decisiones GRANDES del proyecto.
==========================================================================

##########################################################################
#  PENDIENTES (checklist vivo — marcar con [x] al completar)             #
#  Última actualización: 2026-06-28                                      #
##########################################################################

--- PARA CORRER / VALIDAR ------------------------------------------------
[ ] Levantar Docker Desktop (manual) y `docker compose up -d`.
[ ] Correr el flujo E2E con datos demo (login admin/profe/alumno, check-in,
    confirmar, ver panel del alumno). Guía en RUN_LOCAL.md.

--- BACKEND / FUNCIONALIDAD ----------------------------------------------
[ ] Pagos desde el panel admin (registrar/confirmar/cancelar) — hoy solo
    se crean por el seeder demo. Falta endpoint + UI.
[ ] Compra de cuponeras desde admin (crear Pass + crédito en ledger al
    confirmar el pago). Hoy solo por seeder.
[ ] Gestión de deudas / ver morosos (admin).
[ ] Extender vencimiento de cuponeras (admin) — endpoint + UI (la regla R9
    y el ledger 'extension' ya están diseñados).
[ ] Registrar asistencia manual por el admin (sin check-in previo) — UI.
[ ] Subida de FOTOS de alumnos a Supabase Storage (alta con foto + URL
    firmada). Hoy en local se muestran las iniciales.
[ ] Tokens QR DINÁMICOS (Modo B: el alumno escanea el QR de la academia).
[ ] Panel de "pendientes antiguas" + recordatorios al profesor (vista
    attendance_pending_old ya existe).
[ ] Ajustar la vista SQL student_balance para contar también asistencias
    confirmadas NO cubiertas como deuda (D12). Hoy lo calcula el backend
    (StudentSummaryService) pero la vista no.

--- FRONTEND -------------------------------------------------------------
[ ] Iconos PWA (public/icon-192.png, icon-512.png) + service worker.
[ ] Guard por rol en /teacher y /checkin (hoy se apoyan en el 401 del API).
[ ] Mejoras UX del panel del profesor (foto grande, búsqueda, etc.).

--- AUTENTICACIÓN / INFRA ------------------------------------------------
[ ] Migrar / sumar Supabase Auth (hoy auth propia con JWT — decisión D15).
[ ] Aplicar migraciones en un proyecto Supabase real (SIN el stub de auth)
    y configurar cadena de conexión + claves (Storage/Service role).
[ ] Evaluar EF Migrations como alternativa al SQL de db/ (hoy el SQL es la
    fuente de verdad del esquema).

--- DECISIONES DE NEGOCIO A CONFIRMAR (si surgen) ------------------------
[ ] Reglas finas de redondeo/montos de pagos y precios de cuponeras reales.
[ ] Política de auto-expiración de asistencias pendientes no confirmadas.

##########################################################################

--------------------------------------------------------------------------
2026-06-28 — Creación del proyecto y fase de análisis
--------------------------------------------------------------------------
- Se creó la carpeta del proyecto en el Escritorio:
    "APP MOntevideo MAMBO"
  con subcarpeta /docs.

- Se redactó la documentación de análisis (SIN escribir código aún, según
  lo solicitado):
    * README.md ............................ índice y resumen del proyecto.
    * docs/01-ANALISIS-Y-ARQUITECTURA.md ... modelo de dominio, arquitectura,
        flujos (asistencia, cuponeras, pagos), problemas de negocio, casos
        borde, mejoras justificadas, modos de QR, reglas de negocio, auth.
    * docs/02-ESQUEMA-BD.md ................ tablas, PK/FK, índices, vistas,
        RLS y reglas transaccionales para Supabase PostgreSQL.
    * docs/03-SUPABASE-STORAGE-Y-AUTH.md ... fotos en Storage + decisión auth.

DECISIONES GRANDES TOMADAS EN ESTA FASE:
  [D1] Stack: Next.js + .NET 8 Web API + Supabase (Postgres + Storage).
       El backend .NET es la ÚNICA autoridad de escritura de negocio.
  [D2] Autenticación: Supabase Auth para identidad/JWT, validado en .NET.
       Los roles de negocio viven en tablas propias (role/user_role).
  [D3] Asistencia por ventana horaria [fin-15min, fin+30min]; estados
       pending/confirmed/rejected/corrected. Descuento SOLO al confirmar.
  [D4] Cuponeras con LEDGER de movimientos (pass_ledger_entry) en lugar de
       un contador simple. Permite saldo negativo, deuda, correcciones
       reversibles y auditoría. pass.balance es caché desnormalizado.
  [D5] Prioridad de consumo: pase libre (no descuenta) -> pack (FIFO por
       vencimiento) -> clase suelta -> deuda. Nunca se impide asistir.
  [D6] Se separa dance_class (plantilla recurrente) de class_session
       (instancia por fecha) para soportar feriados, suplencias y cancelaciones.
  [D7] Pagos 100% manuales (sin pasarela). Confirmar pago dispara créditos.
  [D8] QR: dos modos soportados (academia escanea alumno / alumno escanea
       academia). QR fijo solo genera "pending", nunca confirma ni descuenta.
       Verificación visual (foto+nombre) para mitigar uso indebido.
  [D9] Fotos en bucket privado student-photos; subida vía backend; lectura
       con signed URLs de corta vida.

PENDIENTE DE CONFIRMAR CON EL NEGOCIO:
  - ¿Una sola sede/salón hoy? (afecta regla de no-solape y multi-room futuro)
  - ¿Modo primario de QR: academia->alumno o alumno->academia?
  - ¿Pase libre por mes calendario o 30 días corridos?

PRÓXIMOS PASOS (no implementados):
  1. Validar el análisis con el cliente.
  2. Cerrar esquema SQL definitivo y migraciones.
  3. Definir grilla de endpoints del Web API.
  4. Prototipo de pantalla de check-in con verificador visual.

--------------------------------------------------------------------------
2026-06-28 — Decisiones de negocio confirmadas por el cliente
--------------------------------------------------------------------------
Se resolvieron las 3 ambigüedades que estaban pendientes:

  [C1] UNA SOLA SEDE, UN SOLO SALÓN (no multisalón).
       => No hay clases simultáneas; la ventana horaria identifica la clase
          sin ambigüedad. La regla de no-solape se simplifica a (weekday +
          rango horario), SIN dimensión `room`. Se deja `room` previsto en
          el modelo pero hoy no se usa para distinguir clases.
       Afecta: 01 §7.1 y §14; 02 §2.5 (room) y §4 (constraint no-solape).

  [C2] LA ACADEMIA ESCANEA AL ALUMNO (Modo A) = MODO PRIMARIO.
       => Se implementa primero el flujo "recepción escanea el QR del
          alumno" con verificación visual (foto+nombre). El Modo B (alumno
          escanea QR de la academia) queda contemplado para el futuro.
       Afecta: 01 §10.

  [C3] VENCIMIENTO = 30 DÍAS CORRIDOS desde la compra (NO mes calendario).
       => valid_to = valid_from + 30 días. Aplica a packs Y al pase libre.
       Afecta: 01 §5.2 y R9; 02 §2.8 (pass_type.validity_days default 30).

Estas decisiones quedan reflejadas en los 3 docs y marcadas como CONFIRMADO.

--------------------------------------------------------------------------
2026-06-28 — Inicio del desarrollo: capa de datos + backend .NET 8
--------------------------------------------------------------------------
Se pasó de diseño a CÓDIGO. Se inicializó repo git en la carpeta del proyecto.

ENTORNO detectado: .NET SDK 10.0.301 (se targetea net8.0), Node 20, npm 10, git.

[E1] BASE DE DATOS (carpeta db/) — migraciones SQL para Supabase PostgreSQL:
      - 001_extensions_and_enums.sql .. pgcrypto, btree_gist, tipo timerange, 7 enums.
      - 002_tables.sql ................ 15 tablas con PK/FK, triggers updated_at.
      - 003_indexes_constraints.sql ... índices + exclusion constraint no-solape
                                        (weekday + rango horario, una sola sede).
      - 004_views_and_triggers.sql .... trigger que mantiene pass.balance desde el
                                        ledger + vistas student_balance y
                                        attendance_pending_old.
      - 005_rls.sql ................... RLS de defensa en profundidad + helpers.
      - seed/001_seed_base.sql ........ roles y tipos de cuponera de ejemplo.
      Migraciones idempotentes. NO se pudo validar contra Postgres real (Docker
      Desktop no estaba corriendo); revisión manual aplicada.

[E2] BACKEND .NET 8 (carpeta backend/) — arquitectura en capas (Mambo.slnx):
      Domain -> Application -> Infrastructure -> Api. COMPILA OK (0 errores).
      - Domain: 15 entidades + enums + reglas puras:
          * AttendanceWindow (ventana [fin-15, fin+30]).
          * ConsumptionPolicy (prioridad pase libre -> pack FIFO -> suelta -> deuda).
      - Application: CheckInService (detección por ventana, anti-dup, fuera de
          ventana -> pendiente) y AttendanceConfirmationService (confirmar 1 /
          confirmar lista / rechazar / corregir por compensación, con auditoría).
      - Infrastructure: MamboDbContext (EF Core + Npgsql, mapeo de enums de PG),
          SystemClock, AuditService, DI.
      - Api: JWT de Supabase (HS256), políticas Admin / TeacherOrAdmin, CORS,
          Swagger. Controllers: CheckIn y Attendance.

[E3] PRUEBAS (tests/Mambo.Domain.Tests) — 12 tests xUnit, TODOS EN VERDE.
      Cubren bordes de la ventana horaria y todos los caminos de la política de
      consumo (pase libre, FIFO, suelta, deuda con/sin cuponera, pase vencido).

DECISIONES DE IMPLEMENTACIÓN (grandes):
  [D10] El esquema SQL de db/ es la FUENTE DE VERDAD del esquema (no EF Migrations).
        EF Core mapea contra ese esquema. Se puede agregar EF Migrations luego.
  [D11] pass.balance se mantiene por DOBLE garantía: la app lo decrementa en memoria
        y un trigger de BD lo recalcula desde el ledger (convergen al mismo valor).
  [D12] "Deuda sin cuponera": si el alumno no tiene ninguna cuponera, la asistencia
        se confirma igual con pass_id NULL y covered_by_unlimited=false (deuda
        implícita / "no cubierta"). PENDIENTE: extender la vista student_balance
        para contar también estas asistencias no cubiertas como deuda.
  [D13] El namespace de casos de uso es Mambo.Application.UseCases (se evitó el
        choque con la entidad Attendance).

PENDIENTE (próximas iteraciones):
  - Frontend Next.js (PWA, scanner QR, paneles por rol).
  - CRUD admin: alumnos, profesores, clases, cuponeras, pagos.
  - Panel del alumno sobre la vista student_balance.
  - Subida de fotos a Supabase Storage (URLs firmadas).
  - Tokens QR dinámicos (Modo B).
  - Ajuste de la vista student_balance por [D12].

--------------------------------------------------------------------------
2026-06-28 — Frontend Next.js (scaffold + pantalla de check-in)
--------------------------------------------------------------------------
[E4] FRONTEND (carpeta frontend/) — Next.js 14 + TypeScript + Tailwind.
      Compila OK (npm run build). Rutas generadas: / y /checkin.
      - lib/supabase.ts: cliente Supabase LAZY (se crea al primer uso para no
        romper el prerender cuando faltan envs) + obtención del JWT.
      - lib/api.ts: cliente del backend .NET que adjunta el Bearer JWT.
      - components/QrScanner.tsx: escáner de QR por cámara (html5-qrcode,
        carga dinámica sin SSR).
      - app/checkin/page.tsx: pantalla de check-in de recepción (modo primario:
        escanea el QR del alumno) con ingreso manual de respaldo y feedback
        (ok / observación / error / idempotencia).
      - app/page.tsx: landing con accesos por rol (resto "próximamente").
      - public/manifest.json + metadata: PWA-ready (faltan iconos y SW).

DECISIÓN: [D14] El cliente Supabase del frontend se inicializa de forma lazy
      con fallback inocuo para que `next build` no falle sin variables de
      entorno. Las envs reales van en .env.local (ver .env.local.example).

ESTADO GENERAL: las TRES capas existen y compilan:
      db/ (SQL Supabase) + backend/ (.NET 8, 12 tests verdes) + frontend/ (Next.js).

--------------------------------------------------------------------------
2026-06-28 — Panel del profesor + verificación visual (cierra el ciclo)
--------------------------------------------------------------------------
Iteración elegida por el cliente. Cierra el flujo de asistencia de punta a punta.

[E5] BACKEND — nuevos casos de uso y endpoints (compila OK):
      - IPhotoStorage + SupabasePhotoStorage: URLs firmadas de fotos vía REST de
        Supabase Storage (degrada a null si no está configurado; no rompe el flujo).
      - StudentSummaryService: resumen del alumno (nombre, foto firmada, clases
        restantes, pase libre, deuda, pendientes). La deuda combina saldos
        negativos + asistencias confirmadas no cubiertas (resuelve parte de D12).
      - CheckInResult ahora incluye StudentId.
      - Endpoints nuevos:
          GET  /api/sessions/today                 (clases de hoy + conteos)
          GET  /api/sessions/{id}/attendances      (lista con verificación visual)
          GET  /api/students/{id}/summary          (resumen del alumno)
          POST /api/checkin/qr  -> ahora devuelve { result, student } (foto+nombre+saldo)
      - AttendanceController quedó solo con acciones (confirm/confirm-many/reject/
        correct); la lista de pendientes se movió a SessionsController.

[E6] FRONTEND — panel del profesor y verificación visual (compila OK):
      - components/StudentCard.tsx: foto (o iniciales) + nombre + saldo/deuda.
      - /checkin: tras escanear muestra la tarjeta del alumno para verificación.
      - /teacher: lista de clases de hoy con pendientes/confirmadas.
      - /teacher/session/[id]: lista de pendientes con tarjeta del alumno;
        confirmar individual, "confirmar toda la lista" y corregir (motivo opcional).
      - Home habilita el acceso al panel del profesor.

CICLO DE ASISTENCIA COMPLETO (E2E a nivel código):
   check-in por QR (recepción) -> pendiente -> panel del profesor -> confirmar
   -> consumo de cuponera / deuda -> corrección por compensación. 12 tests verdes.

PENDIENTE PRIORITARIO PARA PODER PROBARLO CORRIENDO:
   - Login real con Supabase Auth (hoy los endpoints requieren JWT con rol).
   - Datos: aplicar migraciones en un Supabase real + alta de alumnos/clases.
   - Endpoint para alta de alumnos con foto (subida a Storage) y CRUD admin.

--------------------------------------------------------------------------
2026-06-28 — Login, alta de alumnos/clases, panel del alumno y run local
--------------------------------------------------------------------------
[E7] AUTENTICACIÓN PROPIA (decisión [D15]):
      Para poder probar LOCAL antes de configurar Supabase, se implementó auth
      propia con JWT (la alternativa ya documentada en análisis §13). Queda
      preparada para sumar Supabase Auth luego (validación por issuer configurable).
      - password_hash en app_user (migración 006), hashing PBKDF2-SHA256.
      - /api/auth/login devuelve JWT + roles + studentId/teacherId.
      - Program.cs valida JWT propio (Jwt:Key/Issuer/Audience).

[D16] FIX CRÍTICO: se agregó convención snake_case en EF Core
      (EFCore.NamingConventions). Sin esto el DbContext mapeaba a columnas
      PascalCase inexistentes. Ahora FullName->full_name, etc.

[E8] ADMIN + PANEL ALUMNO (backend):
      - AdminService + /api/admin/* : alta de alumnos, profesores, clases y
        "ensure" de sesiones (calcula ventana en UTC, Montevideo = UTC-3).
      - StudentPanelService + /api/me/panel : saldo, cuponeras, historial, pagos.
      - StudentsController /summary y SessionsController ya existentes.

[E9] DEV SEEDER + /api/dev/seed (solo Development):
      Crea datos demo (admin/profe/2 alumnos, clase de hoy con sesión abierta,
      cuponeras —una con saldo y otra agotada para ver deuda—, pago y pendientes).

[E10] FRONTEND: login, sesión propia y vistas por rol:
      - lib/auth.ts (sesión en localStorage) + lib/useAuth.ts (guardia + redirect).
      - api.ts ahora usa el JWT propio (no Supabase).
      - /login (con botón "Cargar datos demo"), home consciente de sesión.
      - /admin, /admin/students, /admin/classes (alta + listado).
      - /me (panel del alumno).
      Compila OK (10 rutas).

[E11] RUN LOCAL preparado:
      - docker-compose.yml: Postgres 16 en localhost:55432, aplica migraciones +
        seed al primer arranque (cada archivo montado en orden; el init de PG no
        recurre subdirectorios).
      - db/_init/00_auth_stub.sql: stub de auth.uid() para Postgres plano (005_rls).
      - backend/src/Mambo.Api/appsettings.Development.json: conexión local + JWT dev
        (se versiona: config local NO secreta).
      - frontend/.env.local: API en 5080. launchSettings: API en 5080.
      - RUN_LOCAL.md: guía paso a paso + credenciales demo.

VERIFICACIÓN: el backend ARRANCA OK (GET /health -> {"status":"ok"}); el login
      llega hasta la capa de datos (falla solo por falta de DB, como se espera).
      NO se pudo levantar Docker Desktop de forma headless (su WSL queda "Stopped";
      requiere abrir la app manualmente). Queda todo listo para `docker compose up`.

PENDIENTE: levantar Docker Desktop (manual) y correr el flujo E2E con datos demo.
      Luego: pagos/cuponeras desde admin (UI), fotos a Storage, y migración a Supabase.

==========================================================================
 (Agregar nuevas entradas debajo, en orden cronológico)
==========================================================================

==========================================================================
 BITÁCORA DE CAMBIOS — APP Montevideo MAMBO (Academia de Baile)
 Se registran aquí los cambios y decisiones GRANDES del proyecto.
==========================================================================

##########################################################################
#  PENDIENTES (checklist vivo — marcar con [x] al completar)             #
#  Última actualización: 2026-07-03 (backlog de mejoras/potenciales)        #
##########################################################################

--- PARA CORRER / VALIDAR ------------------------------------------------
[x] Correr el stack local SIN Docker (Postgres no disponible headless) →
    se agregó modo SQLite de desarrollo. Backend en :5080, front en :3000.
    (2026-07-02) Ver entrada de bitácora del 2026-07-02.
[x] Validar el flujo E2E con datos demo: login admin/profe/alumno OK,
    /api/dev/seed OK, sesiones de hoy con pendientes, panel del alumno con
    saldo/pagos, check-in por QR devuelve tarjeta de verificación. (2026-07-02)
[ ] (Opcional) Levantar Docker Desktop (manual) y `docker compose up -d`
    para validar contra PostgreSQL real cuando WSL/Docker esté disponible.

--- DISEÑO / MARCA -------------------------------------------------------
[x] Rediseño profesional con la identidad MAMBO (negro + verde lima neón,
    ver /Referencias). Sistema de diseño, componentes UI, todas las páginas
    reconstruidas. (2026-07-02) Ver entrada [E12].
[x] Página pública de Horarios 2026 (/horarios) con la grilla de la marca.
[x] Grilla real de clases 2026 sembrada en la BD (POST /api/dev/seed-horarios).
[x] Íconos PWA reales (public/icon-192.png, icon-512.png) + service worker —
    íconos de marca generados (negro + bailarín lima), sw.js (network-first,
    mismo origen) + registro en producción. (2026-07-03) Ver [E26].
[x] Endpoint público de la grilla para que /horarios lea de la BD —
    GET /api/public/schedule (anónimo) + /horarios lo consume con fallback a
    la grilla fija de marca. (2026-07-03) Ver [E26].

--- BACKEND / FUNCIONALIDAD ----------------------------------------------
[x] Pagos desde el panel admin (registrar) — POST /api/admin/payments + UI
    en /admin/payments y en la ficha del alumno. (2026-07-02)
[x] Compra de cuponeras desde admin (Pass + crédito en ledger + pago) —
    POST /api/admin/passes + UI /admin/passes y ficha. (2026-07-02)
[x] Gestión de deudas / ver morosos — GET /api/admin/debtors + UI. (2026-07-02)
[x] Extender vencimiento/clases de cuponeras — POST /api/admin/passes/{id}/
    extend + UI en la ficha del alumno. (2026-07-02)
[x] Registrar asistencia manual por el admin — POST /api/admin/attendance/
    manual + botón en la ficha del alumno. (2026-07-02)
[x] Confirmar/cancelar PAGOS pendientes — POST /api/admin/payments/{id}/
    confirm | cancel + GET /api/admin/payments/pending + UI en /admin/payments
    (checkbox "confirmado" al registrar + lista de pendientes con acciones). (2026-07-03)
[x] Editar / desactivar alumnos y clases — PUT /api/admin/students/{id} y
    /classes/{id} + POST .../active. UI: edición y baja/alta en la ficha del
    alumno y en /admin/classes. (2026-07-03)
[x] Generar sesiones automáticamente para la grilla del día — POST /api/
    sessions/ensure-today (TeacherOrAdmin, idempotente por día). Botón
    "Generar clases de hoy" en el panel del profesor y en el dashboard. (2026-07-03)
[x] Imprimir/mostrar el QR del alumno a partir de su código fijo — componente
    StudentQr (lib qrcode, offline) en la ficha, con impresión de carnet. (2026-07-03)
[ ] Subida de FOTOS de alumnos a Supabase Storage (alta con foto + URL
    firmada). Hoy en local se muestran las iniciales.
[x] Tokens QR DINÁMICOS (Modo B: el alumno escanea el QR de la academia) —
    token rotativo firmado (HMAC, 60s) por clase activa; pantalla /display;
    el alumno marca desde /me (elige la clase + escanea). También muestra su
    propio QR en /me para el Modo A. (2026-07-03) Ver entrada [E19].
[x] Panel de "pendientes antiguas" + recordatorios (profe Y alumno) —
    AlertsService: alumnos en riesgo (cuponera por vencer ≤7d, última clase,
    deuda) y pendientes de clases finalizadas. Banner de avisos en /me y ficha
    (crítico en ROJO), sección Recordatorios en el dashboard y banner de
    pendientes antiguas en /teacher. (2026-07-03) Ver [E27].
[x] Ajustar la vista SQL student_balance (D12) — debt_classes ahora suma también
    las asistencias confirmadas no cubiertas (aplica a Postgres/Supabase; en
    SQLite ya lo calculaba el backend). (2026-07-03) Ver [E27].

--- FRONTEND -------------------------------------------------------------
[x] Guardia de sesión en todas las páginas (useAuth) + barra de navegación
    por rol + notificaciones (toasts) + skeletons. (2026-07-02)
[x] Guard por rol estricto en /checkin (y /teacher, /teacher/session, /display) —
    useAuth acepta una lista de roles; estas páginas exigen admin|teacher en el
    cliente (un alumno es redirigido a /). (2026-07-03) Ver [E26].
[ ] Pull-to-refresh SÓLO sobre el CONTENIDO, no sobre toda la página: el navbar
    de arriba debe quedar FIJO y lo que scrollea/overflow (y desde donde se tira
    para actualizar) es únicamente el área de contenido. Hoy el gesto es sobre la
    ventana entera (el header es sticky pero participa del scroll). (pedido 2026-07-03)
[ ] Confirmar antes de CERRAR SESIÓN (modal "¿Seguro?"): en móvil el botón de
    cerrar el toast/notificación queda en el mismo lugar que el de cerrar sesión,
    y un mistap desloguea sin querer. Agregar confirmación al logout (y/o separar
    los botones). (pedido 2026-07-03)

--- AUTENTICACIÓN / INFRA ------------------------------------------------
[ ] Migrar / sumar Supabase Auth (hoy auth propia con JWT — decisión D15).
[ ] Aplicar migraciones en un proyecto Supabase real (SIN el stub de auth)
    y configurar cadena de conexión + claves (Storage/Service role).
[ ] Evaluar EF Migrations como alternativa al SQL de db/ (hoy el SQL es la
    fuente de verdad del esquema).

--- DECISIONES DE NEGOCIO A CONFIRMAR (si surgen) ------------------------
[ ] Reglas finas de redondeo/montos de pagos y precios de cuponeras reales.
[ ] Política de auto-expiración de asistencias pendientes no confirmadas.

--- BACKLOG DE MEJORAS / POTENCIALES PENDIENTES (detectados 2026-07-03) ---
(No son bloqueantes; se anotan para priorizar más adelante.)

  PRODUCTO
  [ ] App móvil NATIVA (React Native + Expo). Ver PLAN_MOBILE.txt. Es el bloque
      grande si se quiere publicar en tiendas (alumno/profe con tablet/celular).
  [ ] (ALTERNATIVA a discutir) Publicar la PWA en Google Play como TWA
      (Trusted Web Activity) con PWABuilder / Bubblewrap, usando el mismo sitio de
      Vercel. Motivo: al instalar la PWA en algunos Android (viejos o con Chrome/
      Play Services desactualizados) aparece el aviso "Se bloqueó la app / diseñada
      para una versión anterior de Android / sin las protecciones de privacidad más
      recientes". Eso pasa porque la WebAPK la genera Chrome y su targetSdk depende
      del dispositivo (NO se arregla desde nuestro código). Con TWA en Play Store
      controlamos el targetSdk y la firma → se instala sin advertencias en cualquier
      Android. Requiere cuenta de Google Play Developer (US$25 pago único). Genera un
      .aab firmado. Camino mucho más liviano que la app nativa (reusa el mismo front).
      Workaround inmediato para el usuario: actualizar Chrome/Play Services/WebView y
      reinstalar; o usarla desde el navegador sin instalar. (nota 2026-07-04)
  [!] ⚠️ ADVERTENCIA — HOY LA APP ES UNA PWA. Mantenerlo así. Todo lo que se
      construye (contenidos, Mercado Pago, próximamente push) asume PWA: un solo
      código Next servido desde Vercel, service worker propio (public/sw.js),
      manifest.json instalable. Si en algún momento hay que TOCAR algo que obligue
      a dejar de ser PWA y pasar a otra cosa (app NATIVA React Native/Expo, o
      empaquetado TWA/Capacitor/WebView para tienda), eso NO es un ajuste sobre
      lo actual: es un FEATURE APARTE, con su propio proyecto/pipeline/firma y su
      propia decisión de negocio. NO convertir la PWA "sobre la marcha" dentro de
      una tarea que iba de otra cosa. Antes de cualquier cambio que rompa el
      supuesto PWA → parar, avisar y abrir el feature nuevo. (pedido 2026-07-18)
      Casos que disparan esta advertencia (ejemplos, no exhaustivo):
        - Notificaciones push en iOS que exijan app nativa en vez de Web Push.
        - APIs de dispositivo no disponibles en navegador (NFC nativo, biometría,
          background tasks reales, etc.).
        - Requisito de publicar en App Store de Apple (la PWA sola no entra;
          necesita empaquetado tipo TWA/Capacitor o app nativa).
        - Cualquier pedido de "hacela app de verdad / subila a la tienda".
  [x] CRUD de PROFESORES desde la UI — página /admin/teachers (alta, edición
      por modal, baja/alta) + endpoints PUT/active + nav y dashboard. (2026-07-03) [E26]
  [ ] Cancelar / anular cuponeras (hoy sólo se EXTIENDE; falta anular con su
      reverso en el ledger, reason CorrectionReverse/Cancelled).
  [ ] Reportes / métricas: ingresos por período, asistencia por clase, ranking
      de morosos; export CSV de morosos/pagos.
  [ ] Recordatorios / notificaciones: vencimiento de cuponera, deuda, clases
      del día (in-app y/o push cuando haya móvil).
  [ ] Compactar la meta de la card "Clases de hoy" a una línea (menor, estético).

  TÉCNICO / INFRA
  [ ] Configurar Qr:Secret propio en producción (hoy el token del QR Modo B cae
      a la clave JWT si no se define Qr:Secret).
  [ ] Refresh tokens / manejo de expiración de sesión (hoy JWT de 12h sin
      refresco). Importante para móvil y para la UX web.
  [ ] Deploy del backend a la nube + versionado de API (/api/v1). Prerrequisito
      para la app móvil.
  [ ] Paginación / búsqueda server-side en listados (hoy listStudents/listClasses
      traen todo; ok para una academia chica, no escala).
  [ ] Rate limiting / protección de fuerza bruta en el login (hoy sin límite de
      intentos).

  CALIDAD
  [ ] Tests automatizados de los servicios nuevos: BillingService (cuponeras,
      pagos, morosos, pagos pendientes) y QR Modo B (SessionQrService token/scan,
      RegisterForSessionAsync). Hoy validados E2E a mano; el dominio tiene 12 tests.
  [ ] Accesibilidad / foco de teclado en modales (Dialog) y navegación.

##########################################################################

--------------------------------------------------------------------------
2026-06-28 — Creación del proyecto y fase de análisis
--------------------------------------------------------------------------
- Se creó la carpeta del proyecto en el Escritorio:
    "APP MOntevideo MAMBO"
  con subcarpeta /docs.

- Se redactó la documentación de análisis (SIN escribir código aún, según
  lo solicitado):
    * README.md ............................ índice y resumen del proyecto.
    * docs/01-ANALISIS-Y-ARQUITECTURA.md ... modelo de dominio, arquitectura,
        flujos (asistencia, cuponeras, pagos), problemas de negocio, casos
        borde, mejoras justificadas, modos de QR, reglas de negocio, auth.
    * docs/02-ESQUEMA-BD.md ................ tablas, PK/FK, índices, vistas,
        RLS y reglas transaccionales para Supabase PostgreSQL.
    * docs/03-SUPABASE-STORAGE-Y-AUTH.md ... fotos en Storage + decisión auth.

DECISIONES GRANDES TOMADAS EN ESTA FASE:
  [D1] Stack: Next.js + .NET 8 Web API + Supabase (Postgres + Storage).
       El backend .NET es la ÚNICA autoridad de escritura de negocio.
  [D2] Autenticación: Supabase Auth para identidad/JWT, validado en .NET.
       Los roles de negocio viven en tablas propias (role/user_role).
  [D3] Asistencia por ventana horaria [fin-15min, fin+30min]; estados
       pending/confirmed/rejected/corrected. Descuento SOLO al confirmar.
  [D4] Cuponeras con LEDGER de movimientos (pass_ledger_entry) en lugar de
       un contador simple. Permite saldo negativo, deuda, correcciones
       reversibles y auditoría. pass.balance es caché desnormalizado.
  [D5] Prioridad de consumo: pase libre (no descuenta) -> pack (FIFO por
       vencimiento) -> clase suelta -> deuda. Nunca se impide asistir.
  [D6] Se separa dance_class (plantilla recurrente) de class_session
       (instancia por fecha) para soportar feriados, suplencias y cancelaciones.
  [D7] Pagos 100% manuales (sin pasarela). Confirmar pago dispara créditos.
  [D8] QR: dos modos soportados (academia escanea alumno / alumno escanea
       academia). QR fijo solo genera "pending", nunca confirma ni descuenta.
       Verificación visual (foto+nombre) para mitigar uso indebido.
  [D9] Fotos en bucket privado student-photos; subida vía backend; lectura
       con signed URLs de corta vida.

PENDIENTE DE CONFIRMAR CON EL NEGOCIO:
  - ¿Una sola sede/salón hoy? (afecta regla de no-solape y multi-room futuro)
  - ¿Modo primario de QR: academia->alumno o alumno->academia?
  - ¿Pase libre por mes calendario o 30 días corridos?

PRÓXIMOS PASOS (no implementados):
  1. Validar el análisis con el cliente.
  2. Cerrar esquema SQL definitivo y migraciones.
  3. Definir grilla de endpoints del Web API.
  4. Prototipo de pantalla de check-in con verificador visual.

--------------------------------------------------------------------------
2026-06-28 — Decisiones de negocio confirmadas por el cliente
--------------------------------------------------------------------------
Se resolvieron las 3 ambigüedades que estaban pendientes:

  [C1] UNA SOLA SEDE, UN SOLO SALÓN (no multisalón).
       => No hay clases simultáneas; la ventana horaria identifica la clase
          sin ambigüedad. La regla de no-solape se simplifica a (weekday +
          rango horario), SIN dimensión `room`. Se deja `room` previsto en
          el modelo pero hoy no se usa para distinguir clases.
       Afecta: 01 §7.1 y §14; 02 §2.5 (room) y §4 (constraint no-solape).

  [C2] LA ACADEMIA ESCANEA AL ALUMNO (Modo A) = MODO PRIMARIO.
       => Se implementa primero el flujo "recepción escanea el QR del
          alumno" con verificación visual (foto+nombre). El Modo B (alumno
          escanea QR de la academia) queda contemplado para el futuro.
       Afecta: 01 §10.

  [C3] VENCIMIENTO = 30 DÍAS CORRIDOS desde la compra (NO mes calendario).
       => valid_to = valid_from + 30 días. Aplica a packs Y al pase libre.
       Afecta: 01 §5.2 y R9; 02 §2.8 (pass_type.validity_days default 30).

Estas decisiones quedan reflejadas en los 3 docs y marcadas como CONFIRMADO.

--------------------------------------------------------------------------
2026-06-28 — Inicio del desarrollo: capa de datos + backend .NET 8
--------------------------------------------------------------------------
Se pasó de diseño a CÓDIGO. Se inicializó repo git en la carpeta del proyecto.

ENTORNO detectado: .NET SDK 10.0.301 (se targetea net8.0), Node 20, npm 10, git.

[E1] BASE DE DATOS (carpeta db/) — migraciones SQL para Supabase PostgreSQL:
      - 001_extensions_and_enums.sql .. pgcrypto, btree_gist, tipo timerange, 7 enums.
      - 002_tables.sql ................ 15 tablas con PK/FK, triggers updated_at.
      - 003_indexes_constraints.sql ... índices + exclusion constraint no-solape
                                        (weekday + rango horario, una sola sede).
      - 004_views_and_triggers.sql .... trigger que mantiene pass.balance desde el
                                        ledger + vistas student_balance y
                                        attendance_pending_old.
      - 005_rls.sql ................... RLS de defensa en profundidad + helpers.
      - seed/001_seed_base.sql ........ roles y tipos de cuponera de ejemplo.
      Migraciones idempotentes. NO se pudo validar contra Postgres real (Docker
      Desktop no estaba corriendo); revisión manual aplicada.

[E2] BACKEND .NET 8 (carpeta backend/) — arquitectura en capas (Mambo.slnx):
      Domain -> Application -> Infrastructure -> Api. COMPILA OK (0 errores).
      - Domain: 15 entidades + enums + reglas puras:
          * AttendanceWindow (ventana [fin-15, fin+30]).
          * ConsumptionPolicy (prioridad pase libre -> pack FIFO -> suelta -> deuda).
      - Application: CheckInService (detección por ventana, anti-dup, fuera de
          ventana -> pendiente) y AttendanceConfirmationService (confirmar 1 /
          confirmar lista / rechazar / corregir por compensación, con auditoría).
      - Infrastructure: MamboDbContext (EF Core + Npgsql, mapeo de enums de PG),
          SystemClock, AuditService, DI.
      - Api: JWT de Supabase (HS256), políticas Admin / TeacherOrAdmin, CORS,
          Swagger. Controllers: CheckIn y Attendance.

[E3] PRUEBAS (tests/Mambo.Domain.Tests) — 12 tests xUnit, TODOS EN VERDE.
      Cubren bordes de la ventana horaria y todos los caminos de la política de
      consumo (pase libre, FIFO, suelta, deuda con/sin cuponera, pase vencido).

DECISIONES DE IMPLEMENTACIÓN (grandes):
  [D10] El esquema SQL de db/ es la FUENTE DE VERDAD del esquema (no EF Migrations).
        EF Core mapea contra ese esquema. Se puede agregar EF Migrations luego.
  [D11] pass.balance se mantiene por DOBLE garantía: la app lo decrementa en memoria
        y un trigger de BD lo recalcula desde el ledger (convergen al mismo valor).
  [D12] "Deuda sin cuponera": si el alumno no tiene ninguna cuponera, la asistencia
        se confirma igual con pass_id NULL y covered_by_unlimited=false (deuda
        implícita / "no cubierta"). PENDIENTE: extender la vista student_balance
        para contar también estas asistencias no cubiertas como deuda.
  [D13] El namespace de casos de uso es Mambo.Application.UseCases (se evitó el
        choque con la entidad Attendance).

PENDIENTE (próximas iteraciones):
  - Frontend Next.js (PWA, scanner QR, paneles por rol).
  - CRUD admin: alumnos, profesores, clases, cuponeras, pagos.
  - Panel del alumno sobre la vista student_balance.
  - Subida de fotos a Supabase Storage (URLs firmadas).
  - Tokens QR dinámicos (Modo B).
  - Ajuste de la vista student_balance por [D12].

--------------------------------------------------------------------------
2026-06-28 — Frontend Next.js (scaffold + pantalla de check-in)
--------------------------------------------------------------------------
[E4] FRONTEND (carpeta frontend/) — Next.js 14 + TypeScript + Tailwind.
      Compila OK (npm run build). Rutas generadas: / y /checkin.
      - lib/supabase.ts: cliente Supabase LAZY (se crea al primer uso para no
        romper el prerender cuando faltan envs) + obtención del JWT.
      - lib/api.ts: cliente del backend .NET que adjunta el Bearer JWT.
      - components/QrScanner.tsx: escáner de QR por cámara (html5-qrcode,
        carga dinámica sin SSR).
      - app/checkin/page.tsx: pantalla de check-in de recepción (modo primario:
        escanea el QR del alumno) con ingreso manual de respaldo y feedback
        (ok / observación / error / idempotencia).
      - app/page.tsx: landing con accesos por rol (resto "próximamente").
      - public/manifest.json + metadata: PWA-ready (faltan iconos y SW).

DECISIÓN: [D14] El cliente Supabase del frontend se inicializa de forma lazy
      con fallback inocuo para que `next build` no falle sin variables de
      entorno. Las envs reales van en .env.local (ver .env.local.example).

ESTADO GENERAL: las TRES capas existen y compilan:
      db/ (SQL Supabase) + backend/ (.NET 8, 12 tests verdes) + frontend/ (Next.js).

--------------------------------------------------------------------------
2026-06-28 — Panel del profesor + verificación visual (cierra el ciclo)
--------------------------------------------------------------------------
Iteración elegida por el cliente. Cierra el flujo de asistencia de punta a punta.

[E5] BACKEND — nuevos casos de uso y endpoints (compila OK):
      - IPhotoStorage + SupabasePhotoStorage: URLs firmadas de fotos vía REST de
        Supabase Storage (degrada a null si no está configurado; no rompe el flujo).
      - StudentSummaryService: resumen del alumno (nombre, foto firmada, clases
        restantes, pase libre, deuda, pendientes). La deuda combina saldos
        negativos + asistencias confirmadas no cubiertas (resuelve parte de D12).
      - CheckInResult ahora incluye StudentId.
      - Endpoints nuevos:
          GET  /api/sessions/today                 (clases de hoy + conteos)
          GET  /api/sessions/{id}/attendances      (lista con verificación visual)
          GET  /api/students/{id}/summary          (resumen del alumno)
          POST /api/checkin/qr  -> ahora devuelve { result, student } (foto+nombre+saldo)
      - AttendanceController quedó solo con acciones (confirm/confirm-many/reject/
        correct); la lista de pendientes se movió a SessionsController.

[E6] FRONTEND — panel del profesor y verificación visual (compila OK):
      - components/StudentCard.tsx: foto (o iniciales) + nombre + saldo/deuda.
      - /checkin: tras escanear muestra la tarjeta del alumno para verificación.
      - /teacher: lista de clases de hoy con pendientes/confirmadas.
      - /teacher/session/[id]: lista de pendientes con tarjeta del alumno;
        confirmar individual, "confirmar toda la lista" y corregir (motivo opcional).
      - Home habilita el acceso al panel del profesor.

CICLO DE ASISTENCIA COMPLETO (E2E a nivel código):
   check-in por QR (recepción) -> pendiente -> panel del profesor -> confirmar
   -> consumo de cuponera / deuda -> corrección por compensación. 12 tests verdes.

PENDIENTE PRIORITARIO PARA PODER PROBARLO CORRIENDO:
   - Login real con Supabase Auth (hoy los endpoints requieren JWT con rol).
   - Datos: aplicar migraciones en un Supabase real + alta de alumnos/clases.
   - Endpoint para alta de alumnos con foto (subida a Storage) y CRUD admin.

--------------------------------------------------------------------------
2026-06-28 — Login, alta de alumnos/clases, panel del alumno y run local
--------------------------------------------------------------------------
[E7] AUTENTICACIÓN PROPIA (decisión [D15]):
      Para poder probar LOCAL antes de configurar Supabase, se implementó auth
      propia con JWT (la alternativa ya documentada en análisis §13). Queda
      preparada para sumar Supabase Auth luego (validación por issuer configurable).
      - password_hash en app_user (migración 006), hashing PBKDF2-SHA256.
      - /api/auth/login devuelve JWT + roles + studentId/teacherId.
      - Program.cs valida JWT propio (Jwt:Key/Issuer/Audience).

[D16] FIX CRÍTICO: se agregó convención snake_case en EF Core
      (EFCore.NamingConventions). Sin esto el DbContext mapeaba a columnas
      PascalCase inexistentes. Ahora FullName->full_name, etc.

[E8] ADMIN + PANEL ALUMNO (backend):
      - AdminService + /api/admin/* : alta de alumnos, profesores, clases y
        "ensure" de sesiones (calcula ventana en UTC, Montevideo = UTC-3).
      - StudentPanelService + /api/me/panel : saldo, cuponeras, historial, pagos.
      - StudentsController /summary y SessionsController ya existentes.

[E9] DEV SEEDER + /api/dev/seed (solo Development):
      Crea datos demo (admin/profe/2 alumnos, clase de hoy con sesión abierta,
      cuponeras —una con saldo y otra agotada para ver deuda—, pago y pendientes).

[E10] FRONTEND: login, sesión propia y vistas por rol:
      - lib/auth.ts (sesión en localStorage) + lib/useAuth.ts (guardia + redirect).
      - api.ts ahora usa el JWT propio (no Supabase).
      - /login (con botón "Cargar datos demo"), home consciente de sesión.
      - /admin, /admin/students, /admin/classes (alta + listado).
      - /me (panel del alumno).
      Compila OK (10 rutas).

[E11] RUN LOCAL preparado:
      - docker-compose.yml: Postgres 16 en localhost:55432, aplica migraciones +
        seed al primer arranque (cada archivo montado en orden; el init de PG no
        recurre subdirectorios).
      - db/_init/00_auth_stub.sql: stub de auth.uid() para Postgres plano (005_rls).
      - backend/src/Mambo.Api/appsettings.Development.json: conexión local + JWT dev
        (se versiona: config local NO secreta).
      - frontend/.env.local: API en 5080. launchSettings: API en 5080.
      - RUN_LOCAL.md: guía paso a paso + credenciales demo.

VERIFICACIÓN: el backend ARRANCA OK (GET /health -> {"status":"ok"}); el login
      llega hasta la capa de datos (falla solo por falta de DB, como se espera).
      NO se pudo levantar Docker Desktop de forma headless (su WSL queda "Stopped";
      requiere abrir la app manualmente). Queda todo listo para `docker compose up`.

PENDIENTE: levantar Docker Desktop (manual) y correr el flujo E2E con datos demo.
      Luego: pagos/cuponeras desde admin (UI), fotos a Storage, y migración a Supabase.

--------------------------------------------------------------------------
2026-07-02 — Modo SQLite de desarrollo + validación E2E corriendo
--------------------------------------------------------------------------
CONTEXTO: se pidió correr y abrir la app para verla. Docker Desktop NO levanta
      de forma headless en esta máquina (la distro WSL `docker-desktop` queda
      "Stopped" y hay una instalación de WSL en curso que lo bloquea). Para no
      depender de Docker, se agregó un proveedor SQLite para desarrollo.

[D17] PROVEEDOR DE BD SELECCIONABLE (Npgsql | SQLite):
      El backend elige el proveedor por configuración, sin tocar el camino
      Postgres/Supabase (que sigue siendo el objetivo de producción).
      - appsettings.Development.json: "Database:Provider" = "Sqlite" y
        ConnectionStrings:Sqlite = "Data Source=mambo_dev.db".
        Para volver a Postgres: quitar la clave o ponerla en "Npgsql".
      - DependencyInjection.AddMamboInfrastructure: si Provider=Sqlite usa
        UseSqlite(...).UseSnakeCaseNamingConvention(); si no, el camino Npgsql
        con NpgsqlDataSourceBuilder + MapEnum (sin cambios).
      - MamboDbContext.OnModelCreating: los HasPostgresEnum<...> se aplican
        SOLO si Database.IsNpgsql(). En SQLite los enums se guardan como int
        (consistente dentro de la misma BD; el esquema lo crea EF, no el SQL).
      - Program.cs: al arrancar, si el proveedor es SQLite hace
        db.Database.EnsureCreated() (crea el esquema desde el modelo EF).
      - Infra csproj: se sumó Microsoft.EntityFrameworkCore.Sqlite 8.0.10.

      NOTA: en SQLite no corre el SQL de db/ (migraciones/seed/vistas/RLS/
      triggers). Por eso:
        * El catálogo pass_type se siembra ahora también en el DevSeeder
          (EnsureRolesAndPassTypesAsync), replicando db/seed/001_seed_base.sql.
        * La vista student_balance y el trigger de balance NO existen en SQLite;
          el saldo/deuda ya se calculan en el backend (StudentSummaryService),
          así que el flujo funciona igual. Contra Postgres real siguen vigentes.
      Este modo es SOLO para desarrollo/demo local; producción = Supabase.

VERIFICACIÓN E2E (todo corriendo, no solo compilando):
      - Backend .NET en http://localhost:5080 (Swagger en /swagger); esquema
        SQLite creado al arrancar; GET /health -> {"status":"ok"}.
      - POST /api/dev/seed -> datos demo creados.
      - Login OK para admin / profe / alumno (JWT emitido en los tres).
      - GET /api/sessions/today (profe) -> "Salsa Intermedios" con 2 pendientes.
      - GET /api/me/panel (ana@) -> pack 6/8, pago $2200, historial.
      - POST /api/checkin/qr {"qrCode":"STU-ANA-001"} -> devuelve la tarjeta de
        verificación (nombre + saldo) del alumno.
      - Frontend Next.js en http://localhost:3000 (abierto en el navegador).

PENDIENTE: idéntico al checklist de arriba. Lo único que cambió es que ya se
      puede ver/usar la app localmente sin Docker; validar contra Postgres real
      queda opcional para cuando Docker/WSL esté disponible.

--------------------------------------------------------------------------
2026-07-02 — Rediseño profesional de marca + gestión de cuponeras/pagos
--------------------------------------------------------------------------
CONTEXTO: se agregó la carpeta /Referencias con la identidad visual de la
      marca (flyers de horarios y de la fiesta): NEGRO + VERDE LIMA NEÓN,
      logo de pareja bailando en un anillo, slogan "BAILÁ, CONECTÁ, DISFRUTÁ",
      dirección Pablo de María 1474 esq. Brandzen, tel 092136401, y la grilla
      de horarios 2026. Se pidió: (1) que la app se vea totalmente profesional
      con "todos los gadgets posibles" y (2) continuar los pendientes.

[E12] REDISEÑO FRONTEND — SISTEMA DE DISEÑO "MAMBO" (dark + neón):
      - tailwind.config.ts: paleta de marca (lime #C4F82B + escala ink negra),
        sombras con glow, gradientes (lime-grad/hero-grad), animaciones
        (fade-up, shimmer, pulse-glow, spin).
      - globals.css: tema oscuro base, textura de fondo (puntos + glow),
        clases de componentes (.card, .btn-*, .field, .chip-*, .eyebrow).
      - layout.tsx: fuentes Google — Anton (display, estilo póster) + Inter
        (cuerpo). Metadata/manifest de marca. Provider de toasts global.
      - Librería de componentes (src/components/ui/):
          Icons.tsx (SVG inline, sin dependencias), Logo.tsx (marca + anillo),
          Toast.tsx (notificaciones), index.tsx (Button, Card, Stat, Avatar,
          Badge, Skeleton, EmptyState, Field, Spinner), TopBar.tsx (nav por
          rol + Shell + PageHeader). format.tsx (etiquetas ES, fechas UY,
          StatusBadge/PassBadge).
      - TODAS las páginas reconstruidas con la nueva identidad:
          / (landing con hero de marca), /login (accesos rápidos demo),
          /horarios (grilla 2026 pública tipo flyer), /checkin (verificación
          visual grande), /teacher + /teacher/session/[id], /me (panel alumno
          con stats), /admin (dashboard con métricas), /admin/students (+ buscador),
          /admin/students/[id] (ficha completa de gestión), /admin/classes
          (grilla por día), /admin/passes, /admin/payments.
      "Gadgets" sumados: toasts, skeletons de carga, stat tiles, badges,
      avatars con ring, buscador, dashboard con métricas en vivo, hero animado,
      efectos glow, nav sticky responsive (desktop + móvil).

[E13] BACKEND — CUPONERAS, PAGOS Y MOROSOS (BillingService, rol admin):
      Nuevo caso de uso BillingService (registrado en DI) + endpoints en
      AdminController:
        GET  /api/admin/passtypes            catálogo de cuponeras
        POST /api/admin/passes               vender cuponera (Pass + crédito en
                                             ledger + pago opcional confirmado)
        POST /api/admin/passes/{id}/extend   extender días y/o clases (ledger
                                             'extension', nunca edita historia)
        POST /api/admin/payments             registrar pago manual (conf/pend)
        GET  /api/admin/debtors              morosos (saldos negativos + asist.
                                             confirmadas no cubiertas, D12)
        GET  /api/admin/students/{id}        ficha (StudentPanel para gestión)
        POST /api/admin/attendance/manual    asistencia manual (ManualAdmin)
      Respeta las reglas núcleo: todo crédito pasa por el ledger inmutable;
      las extensiones y consumos son movimientos, nunca ediciones.

[E14] HORARIOS 2026 REALES:
      - DevSeeder.SeedHorarios2026Async + POST /api/dev/seed-horarios: carga
        las 23 clases de la grilla oficial (Lun-Vie 18:30/19:30/20:30/21:30 +
        Sábado 14/15/16). Idempotente por (día, hora de inicio). Botón en el
        dashboard admin.

FIX (bug real encontrado al validar E2E):
      [F1] SQLite no soporta ORDER BY sobre 'decimal'. BillingService.
           ListPassTypesAsync ordenaba por Price (decimal) en SQL y tiraba
           System.NotSupportedException. Se corrigió ordenando en memoria
           tras el ToListAsync. (El camino Npgsql no se ve afectado.)

VERIFICACIÓN (todo corriendo, no solo compilando):
      - Backend recompila 0 errores / 0 warnings; frontend `tsc --noEmit` OK;
        todas las rutas responden 200.
      - E2E vía API con token admin: passtypes OK; asignar Pack 8 con pago
        (crédito 8 en ledger); registrar pago manual; extender +15 días +2
        clases (8 -> 10 restantes); asistencia manual idempotente ("ya existía");
        morosos. Ficha del alumno en el front carga OK.
      - App abierta en el navegador (http://localhost:3000).

NOTA: la página /horarios usa la grilla fija de la marca (contenido de
      marketing); las clases operativas viven en la BD (seed-horarios). A
      futuro se puede unificar con un endpoint público de la grilla.

--------------------------------------------------------------------------
2026-07-03 — Gestión operativa: CRUD alumnos/clases, pagos pendientes,
             sesiones del día y carnet QR
--------------------------------------------------------------------------
CONTEXTO: se pidió continuar con los pendientes del checklist. Se cerró un
      lote coherente de gestión operativa (todo verificado corriendo, no sólo
      compilando), respetando las reglas núcleo (backend = única autoridad de
      escritura; ledger inmutable para créditos; sin pasarela de pago).

[E15] BACKEND — EDICIÓN Y BAJA LÓGICA (AdminService + AdminController):
      - Alumnos:  PUT  /api/admin/students/{id}          (nombre, teléfono, doc, notas)
                  POST /api/admin/students/{id}/active    (baja/alta lógica; también
                                                           inhabilita el AppUser)
      - Clases:   PUT  /api/admin/classes/{id}           (horario/estilo/nivel + reemplazo
                                                           del set de profesores)
                  POST /api/admin/classes/{id}/active     (baja/alta lógica; no borra
                                                           sesiones ya generadas)
      - GET /api/admin/students ahora incluye phone; GET /api/admin/classes
        incluye isActive, room y teacherIds (para prellenar la edición).
      Todas las operaciones quedan auditadas (AuditService).

[E16] BACKEND — PAGOS PENDIENTES (BillingService):
      - GET  /api/admin/payments/pending        lista de pagos en estado Pending.
      - POST /api/admin/payments/{id}/confirm    confirma (setea PaidAt/ConfirmedBy;
                                                  si el pago estaba ligado a una
                                                  cuponera, la marca como paga).
      - POST /api/admin/payments/{id}/cancel     cancela (no permite cancelar uno
                                                  ya confirmado).
      El registro de pago (POST /payments) ya soportaba confirmed=false; la UI
      ahora expone el checkbox para dejarlo pendiente.

[E17] BACKEND — SESIONES DEL DÍA (AdminService.EnsureTodaySessionsAsync):
      - POST /api/sessions/ensure-today (política TeacherOrAdmin, para que el
        profesor pueda generarlas). Recorre las clases activas cuyo weekday
        coincide con HOY (fecha local Montevideo = UTC-3) y hace EnsureSession
        de cada una. Idempotente. Devuelve la cantidad de clases del día.

[E18] FRONTEND — UI de todo lo anterior + CARNET QR:
      - Ficha del alumno (/admin/students/[id]): botón Editar (form nombre/tel/doc),
        Dar de baja / Reactivar, badge "Dado de baja", y tarjeta "Carnet QR" con
        el QR generado desde el código fijo (componente StudentQr, lib 'qrcode',
        100% offline) + "Imprimir carnet" (ventana con estilo de marca).
      - /admin/classes: Editar (reusa el form en modo edición, prellena profesores)
        y Baja/Alta por clase, con estilo atenuado + badge "Inactiva".
      - /admin/payments: checkbox "confirmado" al registrar + sección "Pagos
        pendientes de confirmar" con Confirmar / Cancelar por pago.
      - Panel del profesor (/teacher) y dashboard admin: botón "Generar clases
        de hoy" (llama ensure-today y refresca la lista).
      - api.ts: updateStudent, setStudentActive, updateClass, setClassActive,
        ensureTodaySessions, listPendingPayments, confirmPayment, cancelPayment
        + tipos (StudentRow.phone, ClassRow.isActive/room/teacherIds, PendingPayment).
      - Dependencia nueva: qrcode ^1.5.4 (+ @types/qrcode).

VERIFICACIÓN E2E (backend :5080 + frontend :3000 corriendo, SQLite):
      - ensure-today (viernes 2026-07-03) -> 4 clases del día disponibles.
      - PUT alumno -> 204, nombre y teléfono actualizados; baja -> isActive=false;
        alta -> isActive=true.
      - PUT clase (payload ASCII) -> 204, nombre/nivel cambiados y profesores
        reemplazados (1 -> 0); baja/alta de clase -> 204.
      - Pagos: 2 pendientes creados -> listado muestra 2; confirmar uno + cancelar
        el otro -> 0 pendientes; doble-confirmar y cancelar-confirmado devuelven
        400 con mensaje claro (guardas OK).
      - Backend build 0/0; frontend tsc --noEmit OK; next lint sin errores; las 12
        rutas responden 200 (incl. la ficha con el QR).

NOTA (artefacto de shell, NO bug): al probar por curl desde Git Bash, los
      cuerpos JSON con caracteres no-ASCII (ñ, @) se corrompen a nivel shell y
      el backend responde 400 de validación. Con payload ASCII y desde el fetch
      del frontend (UTF-8 real) funciona. Igual criterio que el hallazgo de "Seña".

PENDIENTE (sin cambios de prioridad; ver checklist de arriba): íconos PWA + SW,
      endpoint público de la grilla para /horarios, fotos a Supabase Storage,
      tokens QR dinámicos (Modo B), vista SQL student_balance (D12), y la
      migración a Supabase (Auth + Storage + migraciones reales).

--------------------------------------------------------------------------
2026-07-03 — Modo B: QR dinámico por clase + el alumno marca su asistencia
--------------------------------------------------------------------------
CONTEXTO: se pidió que el ALUMNO sea protagonista del check-in, con dos
      caminos: (A) el alumno muestra su QR y la recepción lo escanea (ya existía
      el backend; faltaba mostrarlo en su cuenta), y (B) el alumno escanea, desde
      su celular, un QR DINÁMICO por clase activa y eso le marca la asistencia.
      Decisiones tomadas con el cliente: QR por clase (no por sede), token
      ROTATIVO firmado (anti-captura), y "tap elige la clase + escaneo obligatorio"
      (prueba de presencia). Ambos caminos terminan en asistencia PENDIENTE que
      el profe confirma (ahí recién se descuenta) — consistente con las reglas.

[E19] BACKEND — TOKEN ROTATIVO + SCAN DEL ALUMNO + PANTALLA:
      - SessionQrService (Application): genera y valida un token por sesión
        con formato MB2.{sessionId}.{slot}.{firma}. slot = floor(unix/60);
        firma = HMAC-SHA256(secreto, "sessionId:slot") (16 hex). Válido sólo para
        el slot actual o el anterior (~2 min) => una foto vieja NO sirve. La firma
        prueba autenticidad (sólo el server la crea); el slot prueba frescura.
        Comparación en tiempo constante. Secreto: config "Qr:Secret" o, en su
        defecto, la clave JWT.
      - CheckInService.RegisterForSessionAsync: check-in sobre una sesión
        EXPLÍCITA (la del token), valida que esté activa [inicio, fin+30min],
        anti-duplicado por (alumno, sesión), crea asistencia Pendiente con
        source = QrStudent (Modo B). IsSessionScannable(...) como regla reutilizable.
      - Endpoints del alumno (MeController, [Authorize]):
          GET  /api/me/qr             mi código de QR fijo (para el Modo A).
          GET  /api/me/active-classes clases corriendo ahora (SIN token).
          POST /api/me/scan {token}   valida el token y marca mi asistencia.
      - Pantalla de la academia (DisplayController, [TeacherOrAdmin]):
          GET  /api/display/active    clases activas CON su token vigente +
                                      rotateInSeconds. Los tokens NO se exponen a
                                      los alumnos (deben escanear).
      - "Activa" se resuelve por instante absoluto (StartAt <= now <= EndAt+30min),
        NO por fecha de calendario, para evitar líos de zona horaria (UTC-3).

[E20] FRONTEND — UI del alumno + display + QR reutilizable:
      - /me: card "Marcar asistencia" (lista de clases activas; tap "Escanear"
        abre la cámara con QrScanner y postea el token) + card "Mi QR" (muestra
        el propio QR con StudentQr para el Modo A).
      - /display: pantalla para la recepción/pantalla del salón; un QR grande por
        clase activa (componente QrImage), refresco cada 15s para mantener el token
        fresco. NavCard "Pantalla QR" en el dashboard admin.
      - components/QrImage.tsx: QR genérico (data URL, offline) a partir de un texto.
      - api.ts: getMyQr, getActiveClasses, scanCheckIn, getDisplayActive + tipos.

VERIFICACIÓN E2E (backend :5080 + frontend :3000, SQLite; DB reseteada para
      tener una clase activa en el horario real de la prueba):
      - display/active (admin) -> 1 clase activa con token + rotateInSeconds.
      - GET /api/me/qr (ana) -> STU-ANA-001; active-classes -> 1 (Salsa Intermedios).
      - Alumno NUEVO escanea token fresco -> asistencia creada (Pending, source
        QrStudent, alreadyExisted:false) y aparece en la lista del profe.
      - Re-escaneo -> idempotente ("Ya habías marcado").
      - Token con firma manipulada -> 400; token con slot viejo -> 400.
      - display/active con token de ALUMNO -> 403 (no acceden a los tokens).
      - Backend build 0/0; frontend tsc + lint OK; /me y /display responden 200.

NOTA OPERATIVA: como el modo SQLite es de desarrollo, el seed de sesión usa
      StartAt=now-80m / EndAt=now+10m para que haya una clase activa al instante;
      el seed es idempotente, así que para reprobar el flujo "en vivo" conviene
      borrar mambo_dev.db (o esperar al horario real de una clase de la grilla).

--------------------------------------------------------------------------
2026-07-03 — Modales de marca en reemplazo de los diálogos nativos
--------------------------------------------------------------------------
[E21] UX — DIÁLOGOS PROFESIONALES (adiós a window.prompt/confirm/alert):
      - Nuevo DialogProvider + hook useDialog() (components/ui/Dialog.tsx) con
        API basada en promesas: confirm({title, message, tone}) => bool y
        prompt({title, fields[]}) => {name: value} | null. Overlay con blur,
        panel estilo .card, cierre por backdrop/Escape, Enter para aceptar,
        autofocus del primer campo. Montado en el layout (dentro de ToastProvider).
      - Se reemplazaron TODOS los diálogos nativos:
          * Profesor / corregir y rechazar asistencia -> modal con textarea de motivo.
          * Ficha del alumno / extender cuponera -> modal con 2 campos (días + clases);
            dar de baja -> modal de confirmación (tono danger).
          * Clases / desactivar -> modal de confirmación (tono danger).
      Verificación: sin ocurrencias de window.prompt/confirm/alert; tsc + lint OK;
      rutas afectadas responden 200.

--------------------------------------------------------------------------
2026-07-03 — Pase de responsive (mobile 360px, ej. Galaxy S8+)
--------------------------------------------------------------------------
[E22] RESPONSIVE — se corrigieron los desbordes horizontales en pantallas
      angostas (360px):
      - /display: el QR era de 320px fijos y se salía de la pantalla. Ahora
        QrImage es responsive (w-full con tope de tamaño) y la caja blanca se
        acota (max-w-[300px]); el QR queda centrado y entero (medido: 262px de
        ancho dentro de 360). Padding del card reducido en móvil.
      - Confirmar/Corregir/Rechazar (asistencias del profe): pasaron de una fila
        de 3 botones a una grilla (Confirmar full-width arriba; Corregir/Rechazar
        abajo). No se aprietan a 360px.
      - PageHeader: las acciones ahora envuelven (flex-wrap) y el título permite
        break-words; el bloque de título usa min-w-0.
      - Cabeceras con nombre/email (mi panel y ficha del alumno): min-w-0 + truncate
        para que un texto largo no empuje el layout.
      - Shell <main> con overflow-x-clip como red de seguridad (no afecta al header
        sticky ni a la grilla de /horarios, que scrollea internamente).
      VERIFICACIÓN: auditoría headless (Chrome, 360x740) de las 14 rutas (admin y
      alumno): scrollWidth == viewport en todas, sin elementos que sobresalgan.
      (La dependencia puppeteer-core usada para medir se quitó luego.)

--------------------------------------------------------------------------
2026-07-03 — Nav: el item seleccionado siempre visible/centrado
--------------------------------------------------------------------------
[E23] La barra de navegación (sobre todo la móvil, con scroll horizontal)
      volvía al inicio al refrescar, dejando el item activo fuera de vista si
      era de los últimos. Ahora el link activo se centra en su barra al cargar
      y al cambiar de ruta (scrollIntoView inline:center, block:nearest, para
      no mover la página en vertical). Aplica a la nav desktop y móvil.
      VERIFICACIÓN (headless, 360px): en /admin/payments (último), /admin/passes
      (medio) y /checkin (primero) el item activo queda siempre visible; los del
      medio quedan centrados, los extremos pegados a su borde visible (no se
      puede centrar el primero/último porque no hay más contenido para desplazar).

--------------------------------------------------------------------------
2026-07-03 — Legibilidad de títulos, selects y responsive de cards
--------------------------------------------------------------------------
[E24] Ajustes de UI pedidos tras probar en mobile (Galaxy S8+):
      - TÍTULOS ILEGIBLES: la base CSS forzaba TODA h1–h4 a la tipografía póster
        (Anton, condensada + tracking negativo), por eso títulos de sección como
        "Vender cuponera" salían apretados y difíciles de leer. Se quitó esa regla:
        ahora los títulos usan Inter (cuerpo) por defecto y el look póster se aplica
        puntual con .font-display (PageHeader, días, hero, etc., que ya lo tienen).
      - BRILLO: se bajó .text-glow (de 24px/0.45 a 12px/0.25) para que los títulos
        con glow (marca/hero) no encandilen.
      - SELECTS: la flecha nativa quedaba pegada al borde. Ahora los <select.field>
        usan una flecha propia (SVG) con margen (~0.85rem) y padding a la derecha.
      - CARDS NO RESPONSIVE (raíz encontrada): varias grillas usaban "grid gap-N"
        SIN columnas base; una grilla con track "auto" crece al max-content e ignora
        el ancho del contenedor (se salía y overflow-x-clip lo ocultaba recortando).
        Se agregó grid-cols-1 a todas las grillas de una sola columna (track
        minmax(0,1fr)) → el contenido se ajusta y trunca bien. Afecta /admin/classes,
        /teacher, ficha, pagos, etc.
      - /admin/classes: cada clase ahora apila los botones (Editar/Baja) debajo con
        un divisor; el badge de nivel queda visible. /teacher: la meta de la clase
        envuelve y la columna de conteos no aplasta el título; hora en 24h.
      VERIFICACIÓN: auditoría headless por elemento (getBoundingClientRect) a 360px
      de las 14 rutas → 0 desbordes de contenido (sólo scrollean, a propósito, la
      barra de nav y la grilla semanal de /horarios). Capturas revisadas.

--------------------------------------------------------------------------
2026-07-03 — Documento de muestra del producto (HTML + PDF)
--------------------------------------------------------------------------
[E25] Se generó una presentación del producto pantalla por pantalla, con
      capturas REALES en móvil (390px) y descripción de "qué tiene / cómo
      funciona" por cada una de las 14 pantallas (público, recepción/QR modos
      A y B, profesor, admin y alumno):
      - PRESENTACION_PRODUCTO.html  (autocontenido; capturas embebidas en base64)
      - PRESENTACION_PRODUCTO.pdf   (A4, 17 páginas; portada de marca + "de un
                                     vistazo" + una pantalla por sección)
      Se armó automatizando Chrome (headless) para loguear cada rol, capturar
      la pantalla completa y componer el HTML con estilo de marca (negro + lima,
      marco de teléfono, listas). Para regenerar: correr backend+frontend, seed
      + seed-horarios (idealmente con la BD reseteada para tener una clase activa),
      y reejecutar el build (usa Chrome del sistema; puppeteer-core temporal).

--------------------------------------------------------------------------
2026-07-03 — Pendientes: horarios desde BD, CRUD profes, guards, PWA
--------------------------------------------------------------------------
[E26] Lote de pendientes cerrado (todo local/SQLite, verificado corriendo):
      - HORARIOS PÚBLICOS DESDE LA BD: PublicController GET /api/public/schedule
        (anónimo) devuelve las clases activas. /horarios pasó a componente cliente
        (HorariosGrid) que arma la grilla desde ese endpoint, con FALLBACK a la
        grilla fija de marca si el backend no responde. Metadata queda en el server
        component. Verificado: 24 clases, grilla y sábado renderizados.
      - CRUD DE PROFESORES: AdminService.UpdateTeacherAsync/SetTeacherActiveAsync
        + endpoints PUT /api/admin/teachers/{id} y POST .../active; lista ahora
        incluye bio. UI nueva /admin/teachers (alta, edición por modal, baja/alta)
        + item de nav "Profesores" + NavCard en el dashboard. Verificado E2E.
      - GUARDS DE ROL: useAuth ahora acepta string | string[]. /checkin, /teacher,
        /teacher/session/[id] y /display exigen admin|teacher en el cliente
        (un alumno queda redirigido a /). Verificado headless.
      - PWA: íconos de marca reales public/icon-192.png y icon-512.png (negro
        full-bleed + bailarín lima, zona segura para maskable), generados con
        Chrome headless. sw.js (cache del app shell + network-first, sólo mismo
        origen; la API pasa directo) + ServiceWorkerRegister (registra en
        PRODUCCIÓN para no interferir con el HMR de dev). Manifest ya referencia
        los íconos; se sumó icons/apple en metadata.
      Backend build 0/0; frontend tsc + lint OK; rutas 200; capturas revisadas.

--------------------------------------------------------------------------
2026-07-03 — Recordatorios/avisos (alumno + profe + admin) y vista D12
--------------------------------------------------------------------------
[E27] RECORDATORIOS con criticidad en ROJO + PENDIENTES ANTIGUAS:
      Reglas únicas en PassAlerts (fuente compartida alumno/admin):
        · CRÍTICO (rojo) — SÓLO pase libre y cuponeras (packs): vencida, vence en
          ≤ 7 días, o queda 1 clase.
        · AVISO (ámbar): vence en ≤ 14 días, o quedan 2 clases.
        · CLASE SUELTA: nunca es crítica. Sólo aviso ámbar "Tenés una clase suelta
          por usar" mientras tenga saldo y esté vigente.
        (Sólo cuponeras activas; "quedan N clases" y "última clase" son sólo para packs.)
      - AlertsService: ListStudentsAtRiskAsync (alumnos con cuponera crítica/por
        vencer) y ListOldPendingAsync (asistencias pendientes de clases ya
        finalizadas; crítico si terminó hace >24h).
      - Los avisos del alumno viajan DENTRO de StudentPanel (Alerts), así /me y la
        ficha del admin los reciben sin llamadas extra; se agrega la deuda como
        aviso crítico.
      - Endpoints: GET /api/admin/alerts { studentsAtRisk, oldPending } y
        GET /api/sessions/pending-old (TeacherOrAdmin).
      - UI: componente AlertsBanner (rojo/ámbar) en /me y ficha; filas de cuponera
        crítica resaltadas en rojo (borde + saldo + "Vence" + badge "Crítico");
        sección "Recordatorios" en el dashboard admin (alumnos en riesgo + link a
        pendientes); banner "Pendientes de clases ya finalizadas" en /teacher.
      - DEMO: el seeder agrega a Ana una cuponera balance 1 y vto +3 días para que
        los avisos críticos se vean de una (reset de mambo_dev.db para tomarlo).

[E28] VISTA SQL student_balance (D12): debt_classes = saldos negativos de
      cuponeras + COUNT de asistencias confirmadas no cubiertas (pass_id null y
      covered_by_unlimited=false). Alinea la vista con lo que ya calcula el
      backend (StudentSummaryService). Aplica al reejecutar db/migrations/004 en
      Postgres/Supabase; SQLite no usa las vistas.

VERIFICACIÓN (corriendo): backend build 0/0; frontend tsc + lint OK; E2E:
      /me/panel de Ana devuelve 2 avisos críticos ("Vence en 3 días", "Queda 1
      clase"); /admin/alerts los agrega; /sessions/pending-old responde 200.
      Capturas de /me y /admin revisadas (rojo correcto).

--------------------------------------------------------------------------
2026-07-03 — Instalación PWA: aviso al primer login + Configuración
--------------------------------------------------------------------------
[E29] Botón/aviso para instalar la app + apartado de Ajustes:
      - usePwaInstall (hook): captura beforeinstallprompt (Android/Chrome/escritorio),
        detecta iOS (instalación manual por Safari) y si ya está instalada (standalone).
      - InstallBanner: aparece la PRIMERA vez tras login (banner de marca con "Instalar
        app" + descartar). Al aceptar o cerrar, guarda mambo.installPromptSeen y NO vuelve
        a aparecer. Se oculta en /settings. Montado en el Shell (todas las páginas con sesión).
      - /settings (Ajustes): ícono de RUEDA en la barra superior (junto a salir). Card
        "Instalar como aplicación" con botón nativo, o instrucciones para iOS / navegador,
        o "ya instalada". Card "Aviso de instalación" con "Volver a mostrar el aviso"
        (limpia el flag para que el banner reaparezca).
      - Íconos nuevos: IconGear, IconDownload, IconShare.
      Recordatorio operativo: la instalación nativa/el service worker requieren HTTPS y
      build de producción; en dev el botón cae a instrucciones. tsc + lint OK; capturas
      revisadas (banner + ajustes).

--------------------------------------------------------------------------
2026-07-03 — Supabase conectado: esquema aplicado + backend verificado
--------------------------------------------------------------------------
[E30] Proyecto Supabase MontevideoMamboProyect (ref cjqxzabpindinvmekzlu):
      - Se generó supabase/deploy.sql (001..006 + seed, SIN el stub de auth local)
        y supabase/migrations/* (para `supabase db push`). Ver SUPABASE_SETUP.md.
      - Esquema aplicado por SQL Editor: tablas, enums, vistas, triggers y RLS OK.
      - Backend .NET verificado contra Supabase (Npgsql, IPv6 + SSL Require):
        conecta, mapea enums, login admin real devuelve JWT. La cadena que sirve:
        Host=db.<ref>.supabase.co;Port=5432;Database=postgres;Username=postgres;
        Password=***;SSL Mode=Require;Trust Server Certificate=true
        (en el deploy va como variable de entorno ConnectionStrings__Supabase; el
         provider como Database__Provider=Npgsql).
      - Datos demo sembrados en Supabase (admin/profe/ana/leo) + grilla 2026 (23).
      - IMPORTANTE (aclaración): el frontend NO usa @supabase/supabase-js ni
        @supabase/ssr; habla con el backend .NET. La publishable/anon key hoy no
        se usa (sólo haría falta con Supabase Auth).

FIX (bug real que SQLite ocultaba):
      [F2] El seed demo creaba la clase "Salsa Intermedios" a las 20:00–21:30 en
           el weekday de hoy, que SE SOLAPA con la grilla 2026. En Postgres la
           restricción de exclusión no_overlap_class (weekday + rango, WHERE
           is_active) lo rechaza (500 23P01); en SQLite no existe esa restricción,
           por eso pasaba. Se movió la clase demo a 12:00–13:30 (fuera de la
           grilla) para que /api/dev/seed y /api/dev/seed-horarios convivan en
           Postgres. En Supabase se resolvió además desactivando la clase demo
           (la restricción sólo aplica a is_active), y así entró la grilla (23).

--------------------------------------------------------------------------
2026-07-03 — Paquete de despliegue gratis (Vercel + Render + Supabase)
--------------------------------------------------------------------------
[E31] Todo listo para publicar gratis:
      - backend/Dockerfile (multi-stage .NET 8) + .dockerignore. Program.cs bindea
        a la variable PORT (Render) sin afectar el local (usa ASPNETCORE_URLS).
      - render.yaml (blueprint del Web Service Docker; secretos con sync:false).
      - README-DEPLOY.md: runbook completo (GitHub → Render backend → Vercel front →
        CORS → prueba), con cómo generar Jwt__Key/Qr__Secret y la connection string.
      - Verificación posible en local: `dotnet publish -c Release` OK (build stage);
        el `docker build/run` no se pudo (daemon apagado, headless) pero el runtime
        contra Supabase ya se probó antes con `dotnet run` (login admin OK). El build
        de la imagen ocurre en Render.
      - Repo (ya existía en main): commit de toda la sesión + el paquete de deploy.
        Sin secretos en el repo (la password de Supabase va como env var en Render).

--------------------------------------------------------------------------
2026-07-03 — Publicado en producción (Vercel+Render+Supabase) + 2 bugs que
             SQLite ocultaba, cazados con smoke test
--------------------------------------------------------------------------
[E32] DEPLOY EN VIVO:
      - Frontend en Vercel (https://montevideomambo.vercel.app), backend en Render
        (https://mambo-api.onrender.com, Docker), base en Supabase.
      - Render (free) NO tiene salida IPv6 y el host directo de Supabase es IPv6 →
        la conexión directa fallaba ("transient failure"). Solución: connection
        string del POOLER (IPv4): Host=aws-1-us-west-2.pooler.supabase.com;Port=5432;
        Username=postgres.<ref>;... (session mode). Región del proyecto: us-west-2.
      - CORS del backend apuntando a la URL real de Vercel (montevideomambo, sin guiones).
      - Verificado en prod: health, schedule (23), login admin, CORS, y smoke test de
        escrituras (20/21 acciones OK; la 21 era la TZ, ya arreglada).

FIX [F3] Asignar cuponera con pago fallaba en Postgres (500, 23503): el movimiento
      del ledger referencia payment_id por FK y EF no ordenaba el pago antes en el
      mismo SaveChanges. SQLite no enforcea FKs → local pasaba. Se guarda el pago
      antes del ledger (misma tx). Commit 69a8379.

FIX [F4] Zona horaria (CRÍTICO): sessions/today, check-in y asistencia manual usaban
      fecha UTC, pero las sesiones se crean con fecha LOCAL (ensure-today). Entre las
      21:00 y medianoche de Montevideo (00:00-03:00 UTC) la fecha UTC ya es el día
      siguiente → 0 sesiones "de hoy" justo en horario de clases. Se agregó
      IClock.LocalToday()/LocalNow() (UTC-3) y se unificó "hoy" = fecha local en
      sessions, check-in, billing, alerts, panel, summary y seeder. Commit 82e0008.

      Lección: SQLite (local) ocultaba ambos (no enforcea FKs; y probábamos de día).
      Validar contra Postgres/Supabase antes de dar por cerrado un flujo.

--------------------------------------------------------------------------
2026-07-03 — [F5] Re-registrar asistencia tras un rechazo
--------------------------------------------------------------------------
[F5] Reportado por el usuario: registrar → rechazar → registrar de nuevo decía
     "ya estaba registrada", cuando debería poder registrarse otra vez.
     El anti-duplicado bloqueaba por CUALQUIER asistencia existente, sin mirar el
     estado. Como hay un unique (uq_attendance_student_session), no se puede
     insertar otra fila; la solución correcta es REABRIR: si la existente está
     Pendiente/Confirmada se bloquea (duplicado real), y si está Rechazada/Corregida
     se vuelve a Pendiente (misma fila, nuevo check-in). Aplica a check-in por QR
     (modo A) y scan del alumno (modo B). El rechazo queda en audit_log.
     Verificado en prod. Commit c80833a.

--------------------------------------------------------------------------
2026-07-03 — Cuponera con pago diferido + cartel Mercado Pago + fix enums
--------------------------------------------------------------------------
[E33] CUPONERA "PAGAR DESPUÉS" (queda como deuda):
      - Al asignar se puede NO cobrar (checkbox aclarado) → la cuponera se entrega
        IMPAGA y queda como deuda del alumno.
      - Nuevo POST /api/admin/passes/{id}/pay (BillingService.PayPassAsync): cobra
        una cuponera impaga (registra el pago por el precio del tipo y la marca paga).
        Botón "Cobrar" en la ficha, en cada cuponera impaga.
      - La cuponera impaga aparece como AVISO (ámbar) "Cuponera impaga: $X" en /me y
        en la ficha (viaja en StudentPanel.Alerts). PassDto ahora incluye Price.
      - Cartel celeste "Próximamente: Mercado Pago" en /admin/payments (placeholder;
        la integración real es a futuro — sin pasarela por ahora, decisión D7).

FIX [F6] enum.ToString() en Selects traducidos a SQL: en Postgres devolvía la
      etiqueta snake_case del enum (class_pack) en vez del nombre C# (ClassPack),
      rompiendo en PRODUCCIÓN los labels/badges del front (kindLabel, StatusBadge,
      "Libre") y las reglas de avisos del backend (kind == "ClassPack"). SQLite lo
      ocultaba (devolvía PascalCase). Se convierte a texto EN MEMORIA (tras
      ToListAsync) en StudentPanelService, BillingService.ListPassTypes y
      AlertsService. Verificado en Supabase: kinds PascalCase + avisos OK.

--------------------------------------------------------------------------
2026-07-03 — Deuda de DINERO (cuponeras impagas) visible en todos lados
--------------------------------------------------------------------------
[E34] Hasta ahora la "Deuda" sólo mostraba CLASES adeudadas; la plata de una
      cuponera entregada sin pagar no aparecía. Ahora:
      - StudentSummary suma DebtMoney = precio de las cuponeras impagas (no
        canceladas). DebtorDto suma DebtMoney; ListDebtors incluye también a los
        que sólo deben plata (antes filtraba por deuda de clases).
      - UI: helper debtDisplay() muestra la deuda de plata ($) y las clases juntas.
        Tile "Deuda" en /me y ficha; badges "Debe $X" en la ficha, en StudentCard
        (lo que ve el profe/recepción) y en la lista de morosos; métrica "Deuda" y
        preview de morosos del dashboard con el monto. Frontend StudentSummary y
        Debtor suman debtMoney.
      Verificado en Supabase: cuponera impaga -> summary.debtMoney=$1200 y aparece
      en morosos aunque no deba clases.

--------------------------------------------------------------------------
2026-07-03 — Pull-to-refresh (tirar para actualizar) en toda la app
--------------------------------------------------------------------------
[E35] Gesto "tirar hacia abajo desde arriba para actualizar" como las apps pro:
      - components/Refresh.tsx: RefreshProvider (registro de recargadores por
        pantalla) + useRegisterRefresh(fn) + PullToRefresh (gesto táctil con
        spinner que baja con el dedo; se activa sólo con scroll arriba de todo;
        umbral 72px; soft-refresh, sin recargar toda la página).
      - Montado global en el layout; globals.css con overscroll-behavior-y:contain
        para que no compita el pull-to-refresh nativo del navegador (que recarga todo).
      - Cada pestaña registra su carga: /admin, /admin/students, /admin/teachers,
        /admin/classes, /admin/passes, /admin/payments, /admin/students/[id],
        /teacher, /teacher/session/[id], /me, /display. (/checkin y /settings no
        tienen datos que recargar.)
      Verificado: emulando el gesto en /admin dispara las 5 recargas de datos. ✅

--------------------------------------------------------------------------
2026-07-03 — Instalación PWA de 1 toque + nombre "Montevideo Mambo"
--------------------------------------------------------------------------
[E36] INSTALAR SIN "COMPARTIR" (Android): el evento beforeinstallprompt de Chrome
      se dispara antes de que monte React y se perdía → el botón caía a instrucciones.
      Ahora se captura en un <script> del <head> (layout) apenas carga la página,
      se guarda en window.__mamboBIP, y usePwaInstall lo lee (+ escucha por si llega
      después). Resultado: en Android el botón "Instalar app" abre el diálogo NATIVO
      con UN toque. Verificado (headless): al tocar se llama a prompt() nativo.
      NOTA: en iOS (iPhone) sigue siendo manual — Apple NO permite instalar por código;
      sólo Compartir → Agregar a inicio (mostramos instrucciones).
      Requisitos en prod OK: manifest, sw.js e íconos servidos (200) + HTTPS.
[E37] Nombre de la app = "Montevideo Mambo" (antes "Montevideo MAMBO — Gestión de
      la academia"). Cambiado en manifest (name/short_name) y metadata (title,
      applicationName, appleWebApp.title). El wordmark visual "MAMBO" del logo queda igual.

==========================================================================
 (Agregar nuevas entradas debajo, en orden cronológico)
==========================================================================

--------------------------------------------------------------------------
2026-07-10 — Análisis de seguridad + endurecimiento de secretos (Grupo 1)
--------------------------------------------------------------------------
[E38] ANÁLISIS DE SEGURIDAD: se documentaron 22 hallazgos (críticos → robustez)
      en ANALISIS_SEGURIDAD.md, con severidad, ubicación (archivo:línea), impacto
      y recomendación, más una tabla de control (columna Estado) y un plan de
      remediación por prioridad. Iremos resolviendo y marcando el estado.

[E39] GRUPO 1 — SECRETOS Y ARRANQUE SEGURO (SEC-01, 02, 04, 22): RESUELTO.
      - SEC-01: se eliminó el fallback de clave JWT hardcodeada en Program.cs y
        JwtIssuer.cs. Nuevo helper ResolveSecret(...) en Program.cs: exige >=32
        chars y, si el secreto falta, usa fallback SOLO en Development; en otro
        entorno LANZA y detiene el arranque (fail-fast). La clave se resuelve una
        vez y se comparte con el emisor vía el nuevo record JwtOptions (fuente
        única de verdad); JwtIssuer ya no relee IConfiguration.
      - SEC-04: el secreto del QR (Qr:Secret) ya NO cae a Jwt:Key. Se resuelve en
        Program.cs con el mismo fail-fast y un fallback dev DISTINTO al del JWT
        (dominios de confianza separados). Se quitó la lógica de DependencyInjection.
      - SEC-02: se quitó Jwt:Key de appsettings.Development.json (quedan Issuer/
        Audience/ExpiresHours, no secretos). Dev funciona por el fallback del código.
      - SEC-22: se quitó la connection string de Postgres con contraseña del
        appsettings.Development.json; el Postgres local de Docker quedó como
        fallback SOLO-dev en DependencyInjection.cs. En prod, si falta la cadena,
        el arranque falla.
      - AddMamboInfrastructure ahora recibe isDevelopment para el fallback de la BD.
      VERIFICADO: build OK (0 warnings); prod sin secretos aborta con mensaje claro;
      prod con secretos por env arranca; dev arranca y login admin emite JWT válido
      (200 con token, 401 sin token).
      PENDIENTE MENOR: la clave dev anterior sigue en el historial de git (era un
      placeholder ya reemplazado y sin uso; los secretos de prod nunca se commitearon).

--------------------------------------------------------------------------
2026-07-10 — Refresh tokens + manejo global de 401 (SEC-08)
--------------------------------------------------------------------------
[E40] SESIÓN CON REFRESH TOKEN (resuelve SEC-08 y el reclamo de UX "vuelve a dar
      401 tras días y no manda a login"). Antes: JWT único de 12 h; al vencer, la
      guardia de rutas dejaba entrar (solo chequeaba presencia en localStorage) y
      cada llamada daba 401 con la UI rota.
      BACKEND:
        - Access JWT corto: Jwt:AccessMinutes (def. 30 min). Refresh token opaco
          largo: Jwt:RefreshDays (def. 30 días). Antes existía Jwt:ExpiresHours (12);
          quedó reemplazado por AccessMinutes/RefreshDays.
        - Nueva tabla refresh_token (entidad EF + mapeo snake_case + IMamboDbContext;
          SQL Postgres en db/migrations/007_refresh_token.sql con RLS sin políticas).
          Se guarda SOLO el hash SHA-256 del token; el valor en claro vive en el cliente.
        - AuthService: LoginAsync ahora emite access+refresh; RefreshAsync rota (revoca
          el usado, emite nuevo) con DETECCIÓN DE REUSO (si llega uno ya rotado, revoca
          toda la cadena del usuario) y valida IsActive; LogoutAsync revoca (idempotente).
        - Endpoints nuevos: POST /api/auth/refresh y POST /api/auth/logout (AllowAnonymous).
        - JwtOptions ahora es (Key, Issuer, Audience, AccessMinutes, RefreshDays);
          se agrega RefreshTokenOptions(Days) para Application. Se resuelven en Program.cs.
      FRONTEND:
        - api.ts: manejo GLOBAL de 401 con single-flight refresh + reintento; si el
          refresh también falla → clearSession + window.location a /login. endSession()
          revoca en el server (best-effort) y limpia; TopBar la usa en "cerrar sesión".
        - auth.ts: Session suma refreshToken/refreshExpiresAt + updateTokens().
      IMPORTANTE (dev): EnsureCreated NO agrega tablas a una BD existente. Para que
      aparezca refresh_token en local, borrar backend/src/Mambo.Api/mambo_dev.db y
      re-sembrar (se regenera con el esquema nuevo).
      VERIFICADO E2E (SQLite): build back+front OK; login→access+refresh; endpoint
      autenticado 200; refresh rota (par nuevo distinto); reuso del refresh viejo→401 y
      corta la cadena; logout→204 y refresh posterior→401; logout idempotente→204;
      token basura→401 (no 500). Tabla refresh_token creada sin errores.

--------------------------------------------------------------------------
2026-07-10 — Endurecimiento: HTTPS/HSTS, rate limiting y seed (SEC-05/07/09)
--------------------------------------------------------------------------
[E41] SEC-05 (HTTPS/HSTS): UseForwardedHeaders (X-Forwarded-Proto/For) para conocer
      esquema/IP reales detras del proxy de Render + UseHsts() en produccion. NO se
      usa UseHttpsRedirection (Render fuerza HTTPS en el borde; a nivel app romperia
      el health-check HTTP y podria hacer bucles). Los forwarded headers ademas dan
      la IP real para el rate limiting.
[E42] SEC-07 (rate limiting): AddRateLimiter con politica "auth" (ventana fija por IP,
      20 intentos / 5 min, excedente -> 429) aplicada a POST /api/auth/login con
      [EnableRateLimiting("auth")]. Se eligio 20 (no 10) por la IP compartida del wifi
      de la academia. VERIFICADO: con limite 10 de prueba, intentos 11-12 dieron 429;
      subido luego a 20. Mejora futura: particion/bloqueo por email + log de fallidos.
[E43] SEC-09 (blindar seed): DevController envuelto en #if DEBUG. El Dockerfile publica
      en Release, asi que en produccion la clase NO existe en el binario (endpoint
      /api/dev/* inalcanzable, no depende de ASPNETCORE_ENVIRONMENT). Se mantiene el
      chequeo IsDevelopment() como segunda barrera. VERIFICADO: en dev (Debug) el seed
      sigue 200.

--------------------------------------------------------------------------
2026-07-10 — CSP estricta + cabeceras de seguridad (SEC-21, mitiga SEC-06)
--------------------------------------------------------------------------
[E44] DECISION SEC-06: front (Vercel) y back (Render) estan en dominios distintos,
      asi que migrar a cookie HttpOnly seria cross-site (SameSite=None + CSRF) y solo
      se valida 100% en prod (riesgo de romper login). Se opto por MITIGAR el robo de
      token con CSP estricta en vez de migrar ahora. La migracion a cookie queda
      recomendada para cuando haya dominio propio (cookies same-site).
[E45] SEC-21 (CSP + headers): nuevo frontend/src/middleware.ts que emite Content-
      Security-Policy con NONCE por request (script-src 'self' 'nonce-...' 'strict-
      dynamic', SIN 'unsafe-inline' para scripts) + X-Content-Type-Options nosniff,
      Referrer-Policy strict-origin-when-cross-origin, X-Frame-Options DENY,
      Permissions-Policy (camara solo self), frame-ancestors 'none'. El nonce se aplica
      al <script> inline de PWA (layout.tsx lee headers()->x-nonce) y Next.js lo propaga
      a sus scripts. connect-src/img-src se arman desde NEXT_PUBLIC_API_URL y
      NEXT_PUBLIC_SUPABASE_URL. Efecto: un XSS no puede ejecutar script inyectado -> no
      puede robar el token de localStorage (mitiga SEC-06; el token sigue en localStorage).
      NOTA: leer headers() en el layout raiz vuelve DINAMICAS todas las rutas (se pierde
      el cacheo estatico de las publicas); aceptable para esta app.
      NOTA DESPLIEGUE (Vercel): NEXT_PUBLIC_API_URL debe estar seteada (ya lo esta) o el
      connect-src bloquearia las llamadas al backend. Si se usan fotos de alumnos
      (Supabase Storage), setear NEXT_PUBLIC_SUPABASE_URL o el img-src bloquearia esas
      imagenes (las iniciales/avatares sin foto no se afectan).
      VERIFICADO: build front OK; cabecera CSP con nonce; 15/15 <script> con nonce (la
      app hidrata); cabeceras de seguridad presentes.

--------------------------------------------------------------------------
2026-07-10 — Fix mobile: carnet QR a pantalla completa + boton Volver
--------------------------------------------------------------------------
[E46] BUG mobile en "imprimir carnet" (StudentQr): abria con window.open + document.
      write y auto window.print(). En el celular se veia DIMINUTO (la ventana no tenia
      <meta viewport>, se renderizaba a ~980px) y era una pestaña nueva SIN forma de
      volver -> el usuario quedaba atrapado. Ahora "Ver / imprimir carnet" NAVEGA a una
      PANTALLA/RUTA PROPIA (/carnet?code=...&name=...): pantalla completa, responsive,
      con la card grande y centrada (border de marca, QR min(78vw,340px)) y la FLECHA de
      volver arriba a la izquierda (router.back). Imprimir aisla la tarjeta #carnet-print
      con @media print en globals.css (visibility + print-color-adjust para el lima; QR a
      tamaño fijo 240px en papel). El boton se usa desde el panel del alumno (/me) y la
      ficha admin (/admin/students/[id]). VERIFICADO: build OK; /carnet responde 200 con
      la card (marca, nombre, codigo, flecha).

--------------------------------------------------------------------------
2026-07-11 — Nav mobile: menu burger + sidebar deslizable (TopBar)
--------------------------------------------------------------------------
[E47] En mobile la navegacion era una fila horizontal con scroll (poco comoda). Ahora
      es un MENU BURGER que abre un SIDEBAR/DRAWER deslizable desde la izquierda
      (animacion slide-in-left + backdrop con fade). Aplica a todos los roles (alumno,
      profe, admin): el drawer arma sus items segun el rol. Incluye: encabezado con logo
      y boton cerrar (X), datos del usuario (avatar+nombre+roles), lista de navegacion
      vertical con item activo resaltado en lima, y al pie Configuracion + Cerrar sesion.
      Cierra al elegir un item, tocar el backdrop, con Escape o al cambiar de ruta; se
      bloquea el scroll de fondo mientras esta abierto. En >=md se mantiene la barra
      horizontal + acciones a la derecha (los iconos settings/logout se ocultan en mobile
      porque viven en el drawer). Se agrego IconMenu y la animacion slide-in-left. Build OK.
      FIX: el drawer no cubria toda la pantalla porque el <header> tiene backdrop-blur
      (backdrop-filter), que crea un containing block y hacia que el `fixed` se posicione
      relativo al header (64px) en vez del viewport. Solucion: renderizar el drawer en un
      PORTAL a document.body (createPortal), fuera del header, con z-[100]. Ahora cubre
      todo el alto desde arriba del todo. Build OK.

--------------------------------------------------------------------------
2026-07-11 — Analisis de rendimiento + to-do (ANALISIS_RENDIMIENTO.md)
--------------------------------------------------------------------------
[E48] Se documentaron 16 items de rendimiento (PERF-01..16) con impacto, ubicacion
      (archivo:linea) y recomendacion, mas una tabla de control (Estado) y un plan por
      costo/beneficio, en ANALISIS_RENDIMIENTO.md. Causas principales de la lentitud:
      - PERF-01 (ALTO): Render plan FREE -> cold starts (~30-60s tras inactividad) +
        0.1 vCPU/512MB. Es casi seguro la causa #1 de la lentitud intermitente. Fix
        rapido: keep-alive (ping a /health cada ~10 min).
      - PERF-02 (ALTO): N+1 en sessions/{id}/attendances (resumen + signed URL por fila).
      - PERF-03 (ALTO): la CSP con nonce (SEC-21) volvio TODAS las rutas dinamicas -> las
        paginas publicas perdieron cacheo estatico/CDN en Vercel (regresion a decidir:
        trade-off seguridad<->rendimiento).
      Medios: StudentSummaryService (~5 queries+HTTP), signed URLs sin cache, listados sin
      paginacion, dashboard admin (5 requests, recalculo sin cache), passes consultado 2x,
      sin cache de datos casi estaticos, fetch-on-mount sin SSR/cache.
      Bajos: AsNoTracking, pooler de Supabase, compresion de respuestas, Cache-Control,
      recomputo de saldo. Los indices de Postgres (db/003) YA cubren las queries calientes.
      Nota: es SOLO analisis + to-do; no se aplicaron cambios de codigo.

--------------------------------------------------------------------------
2026-07-11 — PERF-01: keep-alive del backend (GitHub Action)
--------------------------------------------------------------------------
[E49] Se agrego .github/workflows/keep-alive.yml: cron cada 5 min (repo publico ->
      Actions gratis) que pinguea https://mambo-api.onrender.com/health con reintentos.
      Objetivo: evitar los cold starts de Render free (se apaga tras ~15 min sin trafico;
      el primer hit tarda ~30-60s). Mitiga PERF-01. LIMITACION: el cron de GitHub es
      best-effort y puede demorarse/omitirse, asi que no elimina los cold starts al 100%;
      para algo mas estricto, UptimeRobot o plan pago de Render. Se puede sobreescribir la
      URL con la repo variable KEEPALIVE_URL. VERIFICADO: /health responde 200; la logica
      del ping funciona. El primer run programado puede tardar unos minutos en aparecer en
      la pestana Actions (los workflows nuevos con cron demoran en activarse).

--------------------------------------------------------------------------
2026-07-11 — PERF-05 + PERF-02: cache de signed URLs + fin del N+1
--------------------------------------------------------------------------
[E50] PERF-05: SupabasePhotoStorage cachea la signed URL de cada foto en IMemoryCache
      (AddMemoryCache en DI) por la mitad del TTL (~150s), clave signedurl:{bucket}:{path}.
      Evita el POST HTTP a Supabase por cada foto en cada render. Se agrego el paquete
      Microsoft.Extensions.Caching.Memory 8.0.1 a Infrastructure.
[E51] PERF-02: nuevo StudentSummaryService.GetManyAsync(studentIds) que arma los resumenes
      de varios alumnos en 4 queries FIJAS (students, passes, no-cubiertas agrupadas,
      pendientes agrupadas) + fotos en paralelo (Task.WhenAll, ya cacheadas). Sessions
      Controller.Attendances lo usa en vez del foreach que llamaba GetAsync por fila
      (antes ~5xN queries + N HTTP -> ahora O(1) queries). GetAsync (single) reusa el batch.
      VERIFICADO E2E: /api/sessions/{id}/attendances devuelve HTTP 200 con los resumenes
      correctos (Ana classesRemaining 7, Leo 0, segun seed); /me/panel sigue 200; build y
      12 tests de dominio OK; sin errores en log. Misma forma de respuesta para el front.
      NOTA dev: hay que reiniciar mambo_dev.db si el seed quedo fechado otro dia (la sesion
      demo se crea para "hoy").

--------------------------------------------------------------------------
2026-07-17 — ETAPA 1: validaciones criticas de QR/asistencia/cuponeras
             + primeros tests de la capa Application
--------------------------------------------------------------------------
CONTEXTO: se pidio revisar y endurecer QR, cuponeras y pagos (ademas de un
      backlog nuevo: Mercado Pago, contenidos, redes, push, mapa, responsive
      y tiendas). Al auditar aparecieron bugs REALES de dinero, no solo huecos
      de validacion. Se arreglaron escribiendo primero un test que los reproduce.

[E52] NUEVO PROYECTO tests/Mambo.Application.Tests (37 tests):
      Cierra el pendiente de CALIDAD del checklist ("tests de los servicios
      nuevos"). Hasta ahora los 12 tests cubrian SOLO las 2 clases puras del
      dominio; CheckInService, AttendanceConfirmationService y BillingService
      -- que concentran toda la logica de estado, transacciones y ledger --
      tenian cobertura CERO. Justo ahi vivian los bugs.
      - Harness con SQLite EN MEMORIA sobre el MamboDbContext real (no el
        provider InMemory de EF): respeta las constraints UNIQUE, que es lo
        que varios tests necesitan verificar.
      - El saldo SIEMPRE se afirma contra el LEDGER (fuente de verdad, D4),
        nunca contra el cache pass.Balance.
      - Se agrego InternalsVisibleTo para testear la recuperacion ante la
        carrera de check-in.
      Total: 12 (dominio) + 37 (application) = 49 tests en verde.

[F7] BUG CRITICO — CorrectAsync REGALABA CLASES (fabrica de creditos):
      AttendanceConfirmationService.CorrectAsync no tenia guarda de estado y
      NUNCA limpiaba att.PassId. Cada POST /api/attendance/{id}/correct
      acreditaba otro +1 al ledger, SIN LIMITE, y lo podia hacer cualquier
      profesor. Reproducido: confirmar (-1) + corregir x3 (+3) => saldo +2
      (el alumno terminaba con 2 clases regaladas).
      FIX: (a) solo se corrige lo CONFIRMADO (lo unico que pudo consumir);
           (b) al corregir se limpia PassId/CoveredByUnlimited (la historia
               del consumo y su reverso queda en el ledger, inmutable, no en
               la fila de asistencia).
      Test: Corregir_DOS_VECES_no_regala_clases.

[F8] DOBLE CONFIRMACION CONCURRENTE descontaba DOS clases:
      La guarda "if (att.Status == Confirmed) return" es un read-check-write
      sin lock: dos requests simultaneas (doble clic) la pasaban las dos y
      ambas insertaban un consumo -1. Verificado: sin el fix el test da -2.
      FIX: RECLAMO ATOMICO con ExecuteUpdateAsync (UPDATE condicional
      "WHERE id=@id AND status <> confirmed"): solo una request logra la
      transicion; la que pierde responde idempotente sin volver a descontar.
      Se eligio esto en vez de un indice unico (attendance_id, reason) en el
      ledger porque ese indice ROMPERIA un caso legitimo: confirmar ->
      corregir -> reabrir -> volver a confirmar (segundo consumo valido de la
      misma asistencia). Ventaja adicional: NO requiere cambio de esquema,
      asi que no hay migracion ni hay que recrear mambo_dev.db.
      Requirio sumar Microsoft.EntityFrameworkCore.Relational a Application
      (ExecuteUpdateAsync es de Relational; sigue siendo agnostico del
      proveedor: funciona igual en Postgres y en SQLite).
      Test: Dos_confirmaciones_CONCURRENTES_descuentan_UNA_sola_clase.

[F9] REAPERTURA tras rechazo arrastraba estado sucio (alimentaba F7):
      El camino de reapertura (fix F5) seteaba Status/Source/CheckedInAt pero
      NO limpiaba PassId, ConfirmedAt, ConfirmedBy ni CorrectionReason. La
      fila quedaba con el ConfirmedBy de una confirmacion que ya no existia
      (auditoria mentirosa) y con un PassId obsoleto que habilitaba F7.
      FIX: helper Reopen(...) unico para Modo A y Modo B que limpia TODO el
      rastro de la confirmacion anterior.
      Test: Reabrir_tras_rechazo_limpia_el_rastro_de_la_confirmacion_anterior.

[F10] CARRERA de check-in devolvia HTTP 500:
      Dos check-ins simultaneos leen "no existe" y ambos insertan; el unique
      uq_attendance_student_session corta al segundo con DbUpdateException,
      que NADIE atrapaba -> el alumno veia un error del servidor por tocar
      "escanear" dos veces rapido. La integridad nunca estuvo en riesgo (el
      unique hacia su trabajo); el fallo era de manejo de error.
      FIX: TryInsertAsync atrapa la violacion, relee la fila ganadora y
      responde idempotente ("ya estaba registrada"). Si no hay fila ganadora
      el error NO se silencia (era otra cosa).

[E53] VALIDACIONES QUE FALTABAN:
      - RegisterAsync (lo usa la asistencia manual del admin) ahora valida
        alumno EXISTENTE y ACTIVO. Antes un admin podia registrar asistencia
        de un alumno dado de baja, y un StudentId inexistente reventaba por
        FK -> HTTP 500 en vez de un 400 con mensaje claro.
      - AssignPassAsync valida alumno ACTIVO (antes solo existencia).
      - RejectAsync: no se puede rechazar una CORREGIDA; re-rechazar es
        idempotente y CONSERVA el motivo original (antes lo pisaba y se
        perdia la historia).
      - Confirm idempotente ya no miente en la cobertura: una asistencia
        confirmada como debt_uncovered reportaba "pass" y GeneratedDebt=false.

[D18] DECISION DE NEGOCIO — UNA CUPONERA ACTIVA POR TIPO (confirmada con el
      usuario): AssignPassAsync rechaza entregar una cuponera si el alumno ya
      tiene una ACTIVA y VIGENTE del MISMO tipo. Otros tipos se pueden
      acumular (el consumo es FIFO por vencimiento) y una vencida no bloquea
      la renovacion. Motivo: evitar la doble compra por error y, de cara a
      Mercado Pago, que un reintento del alumno o un webhook repetido le
      cobren dos veces lo mismo. Antes NO habia ninguna validacion (ni en la
      app ni en la BD: la tabla pass no tiene ningun indice unico).

[D19] DECISION — MERCADO PAGO REEMPLAZA A [D7] ("pagos 100% manuales, sin
      pasarela"). El usuario pidio explicitamente compra de cuponeras con
      Mercado Pago, asi que D7 queda SUPERADA. Modelo elegido: CHECKOUT PRO
      (redirect): MP maneja tarjetas/PCI y nosotros solo creamos la
      preferencia y procesamos el webhook. Se descarto Bricks (embebido)
      porque exige abrir la CSP estricta (SEC-21) para cargar su SDK.
      Regla dura: el precio SIEMPRE sale del catalogo en el backend; jamas se
      acepta un importe del cliente. (Ver el matiz de RegisterPaymentAsync en
      PENDIENTES.)

[D20] DECISION — TIENDAS: se va por TWA en Google Play (envolver la PWA con
      Bubblewrap/PWABuilder, ~US$25 pago unico), NO por app nativa Expo. iOS
      queda como PWA instalable. En consecuencia, PUSH se implementa como WEB
      PUSH (VAPID) en sw.js, no FCM/APNs. Esto reemplaza la recomendacion de
      PLAN_MOBILE.txt (React Native + Expo, 6-10 semanas), que queda como
      camino alternativo documentado si mas adelante se quiere App Store.

VERIFICACION: 49/49 tests en verde. Los fixes F7 y F8 se validaron ROMPIENDO
      el fix a proposito y viendo fallar el test (F8 sin el reclamo atomico:
      saldo -2 en vez de -1), para no dejar tests decorativos.

PENDIENTE / A DECIDIR (no se toco por ser cambio de conducta de negocio):
  [ ] AttendanceConfirmationService: la condicion que marca Exhausted es
      redundante (se reduce a Balance == 0). Cambiarla a "<= 0" marcaria
      Exhausted a las cuponeras con saldo NEGATIVO, lo que dejaria
      inalcanzable el caso debt_on_pass (toda la sobre-deuda pasaria a
      debt_uncovered). Ambas variantes cuentan como deuda, pero cambian donde
      se registra. Se dejo la conducta ACTUAL intacta. Requiere decision.
  [ ] TRES VENTANAS HORARIAS DIVERGENTES: Modo A usa [fin-15, fin+30]
      (AttendanceWindow), Modo B usa [inicio, fin+30] (IsSessionScannable) y
      MeController.ActiveClasses reimplementa una tercera inline. El mismo
      alumno, misma clase y mismo instante da resultado distinto segun el
      modo (al minuto 0 el Modo B deja marcar y el Modo A lo manda a
      pendiente por fuera de ventana). Queda DOCUMENTADO en un test
      (Las_ventanas_de_Modo_A_y_Modo_B_son_distintas_a_proposito) para que la
      divergencia sea explicita y no una sorpresa. Requiere decision de
      negocio antes de unificar.
  [ ] RegisterPaymentAsync (POST /api/admin/payments) acepta un importe
      LIBRE del cliente y solo valida Amount > 0, sin contrastarlo con
      PassType.Price aunque venga PassId. Se dejo asi A PROPOSITO: es
      admin-only y los pagos parciales / senias son legitimos. La regla "no
      permitir pagar menos" aplica al camino Mercado Pago, que NO debe reusar
      este endpoint.
  [ ] Deuda sin tope: un pack vigente puede acumular saldo negativo sin
      limite ni alerta dura (es coherente con R5 "nunca impedir asistir",
      pero convendria un umbral de aviso).

--------------------------------------------------------------------------
2026-07-17 — ETAPA 2: Mercado Pago (Checkout Pro) detras de un flag
--------------------------------------------------------------------------
CONTEXTO: se pidio compra de cuponeras con Mercado Pago (ver [D19]). El usuario
      TODAVIA NO tiene credenciales, asi que se dejo la integracion COMPLETA pero
      detras de un flag: si faltan las credenciales, la app funciona igual y la UI
      muestra "proximamente"; alcanza con cargar el AccessToken para activarla.

[E54] BACKEND — compra por pasarela (Checkout Pro, redirect):
      - Nueva entidad PaymentIntent (payment_intent): NUESTRO registro del intento.
        El importe es un SNAPSHOT del catalogo congelado al iniciar el checkout;
        jamas se acepta un monto del cliente. Campos clave: amount, status
        (PaymentIntentStatus pending/approved/rejected/cancelled), preference_id,
        external_payment_id (UNIQUE, ancla de idempotencia), pass_id, payment_id,
        failure_reason.
      - IPaymentGateway (abstraccion en Application) + MercadoPagoGateway
        (Infrastructure). El gateway tiene IsConfigured: si faltan credenciales
        devuelve false y NADA se rompe.
      - CheckoutService (Application) concentra la logica sensible:
          * StartAsync: valida alumno activo + no-duplicado, congela el precio del
            catalogo, crea la preferencia en MP y devuelve el init_point.
          * HandleGatewayNotificationAsync: procesa el webhook. El webhook SOLO trae
            el id del pago; el estado y el IMPORTE se consultan contra la API de MP
            (el cuerpo del webhook no es confiable). Si el pago aprobado vino por
            MENOS que el precio congelado -> se RECHAZA (no se entrega). Idempotente.
          * Reclamo ATOMICO (ExecuteUpdateAsync WHERE status=pending) para que dos
            webhooks simultaneos no entreguen dos cuponeras.
          * Reusa BillingService.AssignPassCoreAsync (nuevo, sin tx propia) para
            entregar la cuponera y marcar el intento en la MISMA transaccion.
      - Endpoints:
          GET  /api/checkout/availability   (anonimo) -> { enabled }  el front decide
                                            entre "comprar" y "proximamente".
          GET  /api/me/passtypes            catalogo comprable (precio del backend).
          POST /api/me/checkout             inicia la compra (solo passTypeId).
          GET  /api/me/checkout             mis compras + estado.
          POST /api/webhooks/mercadopago    (anonimo) webhook. SIEMPRE responde 200
                                            (un 4xx/5xx haria a MP reintentar en loop).
      - Migracion db/migrations/008_payment_intent.sql (enum + tabla + indice UNIQUE
        de idempotencia + trigger updated_at + RLS sin politicas). El SQL de db/
        sigue siendo la fuente de verdad del esquema; en SQLite lo crea EnsureCreated.
        IMPORTANTE (dev): EnsureCreated NO agrega tablas a una BD existente -> para
        ver payment_intent en local hay que borrar mambo_dev.db y re-sembrar.
      - Config (appsettings.json, por variable de entorno en prod, NUNCA en el repo):
          MercadoPago__AccessToken   (obligatorio para habilitar)
          MercadoPago__BackUrl       (a donde vuelve el alumno)
          MercadoPago__NotificationUrl (URL publica del webhook)
          MercadoPago__ApiBase       (override solo para sandbox/tests)

[E55] FRONTEND — compra del alumno + cartel segun estado real:
      - components/BuyPass.tsx: card "Comprar cuponera" en /me. Si el backend
        responde enabled:false -> cartel celeste "Proximamente: Mercado Pago". Si
        esta activo -> lista de cuponeras con su precio y boton Comprar (redirige a
        MP con el init_point) + "Mis compras" con su estado. Al volver de MP con
        ?compra=ok|pendiente|error muestra un toast y refresca (la cuponera la
        entrega el webhook, puede tardar unos segundos).
      - /admin/payments: el cartel fijo de "proximamente" ahora refleja el estado
        REAL (getCheckoutAvailability): "Mercado Pago activo" o "falta cargar la
        credencial".
      - api.ts: getCheckoutAvailability, listMyPassTypes, listMyCheckouts,
        startCheckout + tipo CheckoutIntent. El alumno solo manda passTypeId.

[E56] TESTS — CheckoutService (12 tests nuevos, total 65 en verde):
      Cubren lo critico con una pasarela de mentira (FakeGateway):
      - El precio lo pone el BACKEND (a MP va el del catalogo, no el del cliente).
      - Iniciar checkout NO entrega la cuponera todavia.
      - Pago APROBADO entrega la cuponera (credito por ledger) y registra el pago.
      - WEBHOOK REPETIDO x3 no entrega dos cuponeras (idempotencia).
      - NO se puede pagar de MENOS que el precio real -> se rechaza.
      - Si el catalogo sube DESPUES, el intento respeta su snapshot.
      - Estados rejected/cancelled/pending; un rechazado no se aprueba despues.
      - Webhooks de pagos ajenos o de intentos inexistentes se ignoran.
      - Sin credenciales: StartAsync tira y el webhook no hace nada.

VERIFICACION E2E (backend real + mock de la API de MP, no solo unit tests):
      Se hizo la URL base del gateway configurable (MercadoPago__ApiBase) para
      apuntar a un mock local y ejercitar el MercadoPagoGateway REAL (serializacion
      JSON incluida, que el FakeGateway no cubre). 5 casos, todos OK:
      1. Compra aprobada acredita +8 (Pack 8): 7 -> 15 clases.
      2. Webhook repetido x3: sigue en 15 (NO 39). En BD: UN solo pago mercadopago
         de $2200 pese a 4 webhooks.
      3. Pago aprobado por $100 (< $1200): Leo NO recibe cuponera; intento Rejected
         con motivo "Importe insuficiente".
      4. Pago rejected en MP: no entrega nada.
      5. Compra duplicada del mismo tipo vigente: rechazada.
      Ademas: sin credenciales, GET /api/checkout/availability -> {enabled:false} y
      POST /api/me/checkout -> 503 (la app no se rompe).

NOTA para cuando lleguen las credenciales reales:
      - Cargar MercadoPago__AccessToken (y BackUrl/NotificationUrl con las URLs
        publicas de Vercel/Render) como env vars en Render. Con eso el flag se
        prende solo.
      - Configurar el webhook en el panel de MP apuntando a
        https://<backend>/api/webhooks/mercadopago (topic: payment).
      - Probar primero con credenciales de PRUEBA (sandbox) de MP.
      - Moneda fijada en UYU en la preferencia.


==========================================================================
2026-07-18 — ETAPA 3: GESTIÓN DE CONTENIDOS (novedades/talleres/eventos)
==========================================================================
Objetivo del pedido: que el admin pueda crear/editar/borrar y publicar/ocultar
noticias, novedades, muestras, talleres y eventos (título, descripción, imagen
opcional, fecha, tipo, estado visible/oculto, link externo opcional, ubicación
opcional) y que se vean claramente en la vista del alumno y en una página
pública. La validación sensible (qué es visible) vive SIEMPRE en el backend:
el endpoint público/alumno devuelve SOLO lo publicado; los borradores nunca
salen del admin.

[E57] BACKEND — entidad Content + ContentService + endpoints:
      - Domain/Entities/Content.cs: Id, Type (enum ContentType: News/Update/
        Showcase/Workshop/Event), Title, Body, ImagePath, EventDate (DateOnly?),
        ExternalUrl, LocationName, LocationAddress, Latitude/Longitude (double?),
        IsPublished, CreatedAt/UpdatedAt.
      - Domain/Enums.cs: nuevo ContentType (además de PaymentIntentStatus de E52).
      - Application/UseCases/ContentService.cs:
          ListForAdminAsync   -> incluye borradores (admin).
          ListPublishedAsync  -> SOLO publicados; filtro opcional por tipo.
          CreateAsync / UpdateAsync / SetPublishedAsync / DeleteAsync.
          UpdateAsync: ImagePath null = "no tocar la imagen actual" (para editar
          sin re-subir el flyer). Auditoría (audit_log) en crear/editar/publicar/
          ocultar/borrar.
          MapAsync: si ImagePath ya es una URL http(s) (flyer hosteado afuera) se
          usa tal cual; si es una ruta de Supabase Storage se firma (signed URL,
          300s). Así la "imagen opcional" funciona hoy sin depender de la subida a
          Storage (que sigue pendiente aparte).
      - Api/Controllers/AdminContentController.cs [Authorize Policy=Admin],
        ruta api/admin/content: GET (lista con borradores), POST (crear),
        PUT/{id} (editar), POST/{id}/published (publicar/ocultar), DELETE/{id}.
      - Api/Controllers/PublicController.cs: GET /api/public/content?type=
        (anónimo) -> ListPublishedAsync. El alumno lo consume igual (contenido
        de difusión, no sensible).
      - MamboDbContext + IMamboDbContext: DbSet<Content>; mapeo con
        Type como texto (HasConversion<string>().HasMaxLength(20)) para evitar el
        problema de snake_case de los enums y que el mismo modelo sirva en SQLite
        y Postgres. En Npgsql se agrega el enum nativo.
      - Migración db/migrations/009_content.sql: tabla content (type como text) +
        idx_content_published_type + trigger updated_at + RLS. El SQL de db/ sigue
        siendo la fuente de verdad; en SQLite la crea EnsureCreated.
        IMPORTANTE (dev): EnsureCreated NO agrega tablas a una BD existente -> para
        ver la tabla content en local hay que borrar mambo_dev.db y re-sembrar.

[E58] FRONTEND — admin + vista del alumno + página pública:
      - components/ContentFeed.tsx: muro de contenidos publicados (solo lectura),
        reutilizable. Card con imagen, badge de tipo, fecha, descripción,
        ubicación (IconPin), botón "Cómo llegar" (abre Google Maps) y "Más info"
        (link externo). Modo compact (primeras 3) para /me.
      - app/novedades/page.tsx: página PÚBLICA (sin login) con el feed completo.
      - app/admin/content/page.tsx: ABM con formulario inline (tipo, título,
        descripción, fecha, imagen URL/ruta, link externo, lugar/dirección/lat/lng,
        checkbox publicado) + acciones Editar / Ocultar-Publicar / Borrar
        (confirmación de borrado con el diálogo de marca).
      - app/me/page.tsx: sección "Novedades" con ContentFeed compact + la card de
        compra de cuponera (BuyPass) de la Etapa 2.
      - app/page.tsx (home): botón "Novedades" (sin sesión) y QuickCard (con
        sesión) hacia /novedades.
      - ui/TopBar.tsx: nuevo item de nav "Contenidos" (solo admin) -> /admin/content.
      - components/format.tsx: CONTENT_TYPE_ES + contentTypeLabel + helper mapUrl
        (prioriza lat/lng; si no, la dirección; devuelve URL de búsqueda de Google
        Maps). Este helper es también la base de la Tarea 9 (mapa/ubicación).
      - lib/api.ts: tipos Content/ContentInput/ContentType + listPublishedContent,
        listAdminContent, createContent, updateContent, setContentPublished,
        deleteContent.

VERIFICACIÓN E2E (backend real + SQLite, no solo tipos):
      Se creó un taller PUBLICADO con ubicación (Pablo de María 1474, lat -34.906,
      lng -56.164) y una noticia BORRADOR (isPublished:false). Resultado:
      1. El admin ve los 2 (incluye el borrador).
      2. El público/alumno ve solo 1 (el publicado), con sus datos de ubicación.
      3. Filtro por tipo Workshop -> 1; por tipo Event -> [] (vacío).
      4. Ocultar el taller (POST /published {published:false} -> 204): el público
         pasa a ver [], el admin lo sigue viendo. Publicar de nuevo lo restaura.
      Los 75 tests siguen en verde (12 dominio + 63 Application).

NOTA — cobertura parcial de la Tarea 9 (mapa/ubicación): los contenidos ya
      guardan lugar/dirección/lat/lng y ofrecen "Cómo llegar" a Google Maps.
      Falta (Tarea 9) mostrar la dirección de la academia con link a mapas en
      más lugares de forma centralizada.


==========================================================================
2026-07-18 — REDES SOCIALES + UBICACIÓN CENTRALIZADA + RESPONSIVE ESCRITORIO
==========================================================================
Tres pedidos del backlog resueltos juntos porque comparten la misma base
(datos de marca centralizados). Todo frontend, sin cambios de backend.

[E59] REDES SOCIALES centralizadas (Tarea 3):
      - Nuevo lib/site.ts: ÚNICO lugar con los datos de marca (nombre, slogan,
        dirección, teléfono, URL de mapa) y las redes (WhatsApp, Instagram,
        TikTok, Facebook, sitio web).
      - Criterio (igual que Mercado Pago): cada red se muestra SOLO si tiene URL
        cargada. WhatsApp sale solo (derivado del teléfono real 092 136 401 ->
        wa.me/59892136401). Las demás quedan vacías por defecto y se cargan por
        variable de entorno cuando el cliente pase sus enlaces reales:
          NEXT_PUBLIC_INSTAGRAM_URL, NEXT_PUBLIC_TIKTOK_URL,
          NEXT_PUBLIC_FACEBOOK_URL, NEXT_PUBLIC_WEBSITE_URL,
          NEXT_PUBLIC_WHATSAPP_URL (override opcional), NEXT_PUBLIC_MAPS_URL.
        No se inventan cuentas: sin URL, no se muestra el icono.
      - Iconos nuevos en ui/Icons.tsx: IconInstagram, IconWhatsapp, IconFacebook,
        IconTiktok, IconGlobe (glifos de marca; rellenos donde ayuda a reconocerlos).
      - components/SiteFooter.tsx: <SocialLinks/> (fila de iconos, solo las
        configuradas) + <SiteFooter/> (pie con dirección enlazada a mapa, teléfono
        y redes). Reutilizable.
      - Se muestran en: home (/), /novedades, grilla /horarios y /horarios/[dia],
        y en el drawer móvil del TopBar (usuarios logueados).

[E60] UBICACIÓN / MAPA (Tarea 9 — completada):
      - SITE.mapsUrl: link a Google Maps con la dirección de la academia
        (Pablo de María 1474 esq. Brandzen). La dirección pasó de texto plano a
        ENLACE en todos los pies de página. Google Maps universal funciona tanto
        en Android (abre Google Maps) como en iOS (abre Maps/navegador).
      - Se elimina la duplicación: antes la dirección/teléfono estaban hardcodeados
        en 4 páginas; ahora salen de SITE.
      - Complementa lo ya hecho en la Etapa 3: los contenidos (talleres/eventos/
        muestras) ya tenían lugar + lat/lng + botón "Cómo llegar" (helper mapUrl
        en format.tsx, que prioriza lat/lng y cae a la dirección).

[E61] RESPONSIVE ESCRITORIO / TABLET (Tarea 4):
      - El navbar se veía "estirado tipo móvil" en pantallas grandes (alto fijo
        h-16 y todo en text-sm). Ahora escala a partir de lg:
          header: h-16 -> lg:h-20, contenedor max-w-6xl -> xl:max-w-7xl,
                  padding lg:px-6, gaps mayores.
          ítems de nav: lg:px-4 lg:py-2.5 lg:text-[15px], iconos lg:text-lg.
          logo: prop `large` -> LogoMark lg:h-10 y wordmark MAMBO lg:text-2xl.
          bloque de usuario y botones de acción (engranaje/salir): lg:h-10/lg:w-10
                  y texto lg:text-[15px].
          contenedor de contenido (Shell): lg:px-6 lg:py-10 (más respiro).
      - Móvil queda igual (drawer a pantalla completa como antes).

Verificación: tsc --noEmit OK, next lint 0 warnings, next build OK (21 rutas).


==========================================================================
2026-07-18 — NOTIFICACIONES PUSH (Web Push / VAPID) — Tarea 8
==========================================================================
Decisión [D20]: Web Push (VAPID), NO app nativa. Encaja con la PWA (Android y
iOS 16.4+ con la PWA instalada soportan Web Push) y respeta la advertencia de no
salir de PWA (ver backlog PRODUCTO). Si en el futuro se necesita push en iOS que
exija app nativa, eso sería un FEATURE APARTE, no un ajuste sobre esto.

Igual que Mercado Pago: si NO hay claves VAPID configuradas, la función está
apagada (IsConfigured=false) y la UI ni siquiera ofrece activarla. La app no se
rompe. Cripto delegada a la librería WebPush (aes128gcm + firma VAPID); NO se
hace cripto a mano.

[E62] BACKEND — suscripciones + envío + disparadores:
      - Domain/Entities/Push.cs: entidad PushSubscription (Id, UserId, Endpoint
        ÚNICO, P256dh, Auth, UserAgent, CreatedAt, LastUsedAt, FailureCount).
        Un dispositivo/navegador = una fila; re-suscribir ACTUALIZA (no duplica).
      - Application/Abstractions/IPushSender.cs: PushDevice, PushMessage,
        enum PushDeliveryResult { Ok, Gone, Error }, IPushSender (IsConfigured,
        PublicKey, SendAsync). Application depende de la abstracción, no de la lib.
      - Application/UseCases/PushService.cs:
          SubscribeAsync (upsert por endpoint; valida datos completos),
          UnsubscribeAsync, SendToUserAsync, SendToRoleAsync (ej. todos los
          alumnos), SendToAllAsync. DispatchAsync es BEST-EFFORT: nunca lanza
          (una notificación no debe romper la operación que la dispara) y limpia
          sola las suscripciones vencidas (Gone=404/410) o con muchos fallos.
      - Infrastructure/Services/WebPushSender.cs (IPushSender): usa la librería
        WebPush 1.0.12 (+ Portable.BouncyCastle). Serializa el payload
        { title, body, url, tag } y mapea 404/410 -> Gone. IsConfigured = hay
        clave pública Y privada.
      - Api/Controllers/PushController.cs:
          GET  /api/push/vapid-public-key   (anónimo) -> { enabled, publicKey }.
          POST /api/push/subscribe          (auth) registra ESTE dispositivo al
                                            usuario del token. 503 si no configurado.
          POST /api/push/unsubscribe        (auth) da de baja el dispositivo.
          POST /api/push/test               (auth) notificación de prueba a uno mismo.
          POST /api/admin/push/broadcast    (Admin) difusión: all | students | teachers.
      - Disparadores REALES cableados (best-effort, fuera de transacción):
          * Contenido publicado -> aviso a TODOS los alumnos (ContentService, al
            crear publicado o al pasar de borrador a publicado; no re-avisa).
          * Asistencia confirmada -> aviso a ESE alumno (AttendanceConfirmationService,
            solo cuando realmente transiciona a Confirmed; no en confirmaciones repetidas).
          * Pago confirmado -> aviso a ESE alumno (BillingService.ConfirmPaymentAsync).
        PushService se inyecta como OPCIONAL (nullable) en los servicios
        transaccionales para no obligar a los tests unitarios a pasarlo.
      - IMamboDbContext + MamboDbContext: DbSet<PushSubscription>; mapeo con
        índice ÚNICO en endpoint + índice por user_id + FK a app_user con cascade.
      - DI: IPushSender -> WebPushSender (singleton), PushService (scoped).
      - Migración db/migrations/010_push_subscription.sql (tabla + índice único
        de endpoint + índice por user + RLS). En SQLite la crea EnsureCreated.
        IMPORTANTE (dev): EnsureCreated NO agrega tablas a una BD existente -> para
        ver push_subscription en local hay que borrar mambo_dev.db y re-sembrar.
      - Config (appsettings.json; por variable de entorno en prod, NUNCA en el repo):
          Push__VapidPublicKey   (base64url; también la usa el frontend)
          Push__VapidPrivateKey  (base64url; SECRETO)
          Push__Subject          (mailto:... o URL de contacto; requerido por el estándar)

[E63] FRONTEND — service worker + suscripción + UI:
      - public/sw.js: nuevos handlers 'push' (muestra la notificación con
        title/body/icon/tag) y 'notificationclick' (enfoca o abre la app en la url).
        El resto del SW (cache del app shell) sin cambios. La PWA sigue igual.
      - lib/push.ts: pushSupported(), currentPushState(), enablePush()
        (pide permiso + PushManager.subscribe con la clave pública VAPID + manda
        endpoint/keys al backend), disablePush().
      - components/PushSettings.tsx: en /settings, toggle de notificaciones por
        dispositivo (activar / prueba / desactivar) para cualquier usuario, y para
        ADMIN un formulario de difusión (todos / alumnos / profesores). Si el
        backend no tiene claves, no se muestra nada (igual criterio que MP).
      - lib/api.ts: getVapidKey, subscribePush, unsubscribePush, testPush, broadcastPush.
      - ui/Icons.tsx: IconBell.

TESTS (7 nuevos; total 82: 12 dominio + 70 Application):
      PushServiceTests con un FakePushSender (no sale a la red):
      - Suscribir dos veces el MISMO endpoint no duplica (y actualiza el user agent).
      - Suscripción incompleta se rechaza.
      - Enviar al usuario llega a TODOS sus dispositivos.
      - Enviar por ROL llega solo a los alumnos (no a otros roles).
      - Una suscripción vencida (Gone) se borra sola tras el envío.
      - Sin configuración no envía nada.
      - Desuscribir borra el dispositivo.

VERIFICACIÓN E2E (backend real, SQLite):
      - Sin claves: GET /api/push/vapid-public-key -> {enabled:false}; POST
        /api/push/subscribe sin token -> 401 (la app arranca OK: la DI de los
        servicios push nuevos no rompe el arranque).
      - Con un par VAPID real (generado con la librería): GET vapid-public-key ->
        {enabled:true, publicKey:...}. El flag se prende solo al cargar las claves.
      NOTA: la ENTREGA real de la notificación solo se puede probar en un navegador
      con HTTPS (y en iPhone con la PWA instalada, iOS 16.4+); no es verificable
      headless porque hace falta una suscripción real del navegador.

GENERAR EL PAR DE CLAVES VAPID (una sola vez):
      Con la librería WebPush ya incluida:
        var k = WebPush.VapidHelper.GenerateVapidKeys();
        // k.PublicKey  -> Push__VapidPublicKey  (y NEXT_PUBLIC no hace falta: la
        //                 pública la sirve el backend en /api/push/vapid-public-key)
        // k.PrivateKey -> Push__VapidPrivateKey (SECRETO)
      Cargarlas como variables de entorno en Render (backend). Con eso el flag se
      prende solo. El Subject debe ser un mailto: o URL de contacto real.

PENDIENTE (no bloqueante) para cerrar la Tarea 8 al 100%:
      - Recordatorios de clase y avisos de DEUDA: hoy AlertsService ya calcula
        "alumnos en riesgo" y "pendientes antiguas", pero no hay un JOB programado
        que dispare el push. Falta un cron/hosted service que llame a un envío de
        recordatorios (se puede reusar AlertsService + PushService.SendToUserAsync).
        La CAPACIDAD de enviar ya está; falta el disparador temporal.
      - Aviso de "compra aprobada" por Mercado Pago (CheckoutService): fácil de
        sumar cuando MP esté activo (reusa PushService.SendToUserAsync).

==========================================================================
2026-07-18 — INFORME DE PUBLICACIÓN EN TIENDAS (Tarea 5, sin publicar)
==========================================================================
[E64] Nuevo docs/04-PUBLICACION-TIENDAS.md: relevamiento de qué falta para
      publicar en Google Play y App Store (NO se publica nada). Resumen:
      - La PWA ya es instalable en Android; el problema es solo el aviso de
        "versión anterior de Android" en algunos equipos.
      - Camino A recomendado: Google Play como TWA (Bubblewrap/PWABuilder),
        US$25 pago único, reusa el front. Camino B (App Store) exige wrapper
        nativo (Capacitor) o app nativa + Mac + US$99/año = FEATURE APARTE
        (respeta la advertencia PWA del backlog).
      - BLOQUEANTES legales para CUALQUIER tienda (hoy faltan): página de
        política de privacidad + términos, y BORRADO DE CUENTA in-app (Google y
        Apple lo exigen). Además faltan screenshots y textos de ficha, y el
        assetlinks.json para la TWA.
      - Riesgo de pagos: al ser clases PRESENCIALES (servicio real) y con MP por
        REDIRECCIÓN (Checkout Pro), se permite cobrar por fuera de la tienda.
      - Orden sugerido: 1) privacidad/términos 2) eliminar cuenta 3) assets de
        ficha 4) TWA + Play Console 5) (futuro) iOS.

==========================================================================
2026-07-18 — FIX scanner QR (crash) + marcado DEMO sin QR
==========================================================================
[E65] FIX crash del escáner de QR: al abrir la cámara y no arrancar (permiso
      denegado / sin cámara), html5-qrcode lanzaba "Cannot stop, scanner is not
      running or paused" de forma SINCRÓNICA (el .catch async no la atrapaba) y
      tumbaba toda la app sin botón para volver. Ahora QrScanner:
      - solo llama stop() si getState() es SCANNING/PAUSED; si no, clear();
      - todo envuelto en try/catch (sync + async);
      - el callback onScan va en un ref, así el efecto NO se re-monta en cada
        render (antes el scanner se reiniciaba constantemente).
      + Nuevo app/error.tsx (error boundary): si una pantalla crashea, muestra
        "Ups, algo falló" con botones Reintentar / Volver al inicio, en vez de la
        pantalla trabada sin salida.

[E66] MARCADO DEMO sin QR (para el ambiente desplegado con usuarios mockeados):
      Los usuarios demo (correo @mambo.local) no están en la academia ni tienen
      el QR. Ahora, en "Marcar asistencia", en vez del botón Escanear ven
      "Marcar (demo)" que marca directo la clase activa elegida, SIN cámara.
      - Backend: POST /api/me/scan-demo { sessionId }. Valida en el SERVIDOR que
        el correo del usuario termine en @mambo.local (403 si no) y que la sesión
        esté realmente activa (misma ventana que active-classes). Reusa el mismo
        RegisterForSessionAsync que el escaneo real → misma lógica, anti-duplicado.
      - Frontend: me/page detecta el usuario demo por su email y muestra el botón
        de marcado simulado; el escáner queda solo para usuarios reales.
      Verificado E2E: ana@mambo.local marca sin QR (queda pendiente, repetir no
      duplica); un usuario con email real recibe 403.

==========================================================================
2026-07-18 — FIX: no permitir marcar dos veces la misma clase
==========================================================================
[E67] active-classes ahora incluye myStatus: el estado de asistencia del alumno
      en cada clase activa (null si todavía no marcó). El front, si ya está
      Pending o Confirmed, muestra el estado ("Pendiente"/"Asistida") en lugar
      del botón Escanear/Marcar (demo). Antes, tras marcar y confirmar, al volver
      aparecía de nuevo el botón y parecía que no había marcado.
      Verificado E2E: null -> Pending (al marcar) -> Confirmed (al confirmar el profe).

==========================================================================
2026-07-18 — FIX: estado de carga en "Cerrar sesión"
==========================================================================
[E68] El botón de cerrar sesión (escritorio y drawer móvil) era un <button>
      plano sin feedback: con el backend lento (cold start de Render) uno no
      sabía si estaba pasando algo y martillaba el botón. Ahora muestra un
      spinner ("Cerrando sesión…" en el drawer), se deshabilita mientras corre y
      la función ignora clics repetidos (guarda loggingOut). El resto de los
      botones de acción ya usaban <Button loading> con spinner.
